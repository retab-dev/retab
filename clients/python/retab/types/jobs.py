# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Any, Literal
from pydantic import BaseModel, ConfigDict, Field


JobStatus = Literal["validating", "queued", "in_progress", "completed", "failed", "cancelled", "expired"]


SupportedEndpoint = Literal[
    "/v1/extractions",
    "/v1/parses",
    "/v1/splits",
    "/v1/partitions",
    "/v1/classifications",
    "/v1/schemas/generate",
    "/v1/edits",
    "/v1/edits/templates/generate",
    "/v1/evals/extract/process",
    "/v1/evals/extract/extract",
    "/v1/evals/extract/split",
]


CreateJobRequestEndpoint = SupportedEndpoint


class CreateJobRequest(BaseModel):
    """Request body for POST /v1/jobs."""

    model_config = ConfigDict(extra="forbid", populate_by_name=True, protected_namespaces=())

    endpoint: SupportedEndpoint
    request: dict[str, Any]
    metadata: dict[str, str] | None = Field(default=None, description="Max 16 pairs; keys ≤64 chars, values ≤512 chars")


class Job(BaseModel):
    """Core Job object following OpenAI-style specification.

    Represents a single asynchronous job that can be polled for status
    and result retrieval."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str | None = None
    object: Literal["job"] = Field(default="job")
    status: JobStatus | None = Field(default="validating")
    endpoint: SupportedEndpoint
    error: JobError | None = None
    warnings: list[JobWarning] | None = None
    created_at: str | None = None
    started_at: str | None = None
    completed_at: str | None = None
    expires_at: str | None = None
    metadata: dict[str, str] | None = None
    cancelled: bool | None = Field(default=False)
    attempt_count: int | None = Field(default=0)
    last_attempt_at: str | None = None
    last_failure_code: str | None = None
    request: dict[str, Any] | None = None
    response: JobResponse | None = None


class JobError(BaseModel):
    """Error details when job fails."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    code: str
    message: str
    details: dict[str, Any] | None = None


class JobWarning(BaseModel):
    """Warning details when non-fatal issues occur."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    code: str
    message: str
    details: dict[str, Any] | None = None


class JobResponse(BaseModel):
    """Public response returned when job completes successfully."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status_code: int
    body: dict[str, Any]


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
CreateJobRequest.model_rebuild()
Job.model_rebuild()
JobError.model_rebuild()
JobWarning.model_rebuild()
JobResponse.model_rebuild()
