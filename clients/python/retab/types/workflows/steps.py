# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from enum import Enum
from typing import Any, Literal
from pydantic import BaseModel, ConfigDict, Field
from retab.types.mime import FileRef
from retab.types.workflows.artifacts import ErrorDetails
from retab.types.workflows.blocks.executions import StepArtifactRef


class ErrorStepLifecycleStage(str, Enum):
    INPUT_COLLECTION = "input_collection"
    REGISTRY_LOOKUP = "registry_lookup"
    DOCUMENT_FETCH = "document_fetch"
    EXECUTION = "execution"
    OUTPUT_STORAGE = "output_storage"
    ROUTING = "routing"
    HISTORY_PAYLOAD = "history_payload"


class ErrorStepLifecycleCategory(str, Enum):
    TRANSIENT = "transient"
    PERMANENT = "permanent"
    QUOTA = "quota"


class PublicHandlePayloadType(str, Enum):
    FILE = "file"
    JSON = "json"


class WorkflowRunStepBlockType(str, Enum):
    START_DOCUMENT = "start_document"
    START_JSON = "start_json"
    NOTE = "note"
    PARSE = "parse"
    EDIT = "edit"
    EXTRACT = "extract"
    SPLIT = "split"
    CLASSIFIER = "classifier"
    CONDITIONAL = "conditional"
    API_CALL = "api_call"
    FUNCTION = "function"
    WHILE_LOOP = "while_loop"
    FOR_EACH = "for_each"
    MERGE_DICTS = "merge_dicts"
    WHILE_LOOP_SENTINEL_START = "while_loop_sentinel_start"
    WHILE_LOOP_SENTINEL_END = "while_loop_sentinel_end"
    FOR_EACH_SENTINEL_START = "for_each_sentinel_start"
    FOR_EACH_SENTINEL_END = "for_each_sentinel_end"


class AwaitingReviewStepLifecycle(BaseModel):
    """The step is paused on a human review decision."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["awaiting_review"] = Field(default="awaiting_review")


class CancelledStepLifecycle(BaseModel):
    """The step was cancelled."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["cancelled"] = Field(default="cancelled")
    reason: str = Field(..., description="Reason the step was cancelled")


class CompletedStepLifecycle(BaseModel):
    """The step finished successfully."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["completed"] = Field(default="completed")


class ContainerContextData(BaseModel):
    """Structured context for a single container in the hierarchy."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    container_id: str = Field(..., description="Container ID (e.g., 'while_loop-abc')")
    iteration: int = Field(..., description="Iteration index (0-based)")
    is_parallel: bool | None = Field(default=False, description="Whether this container represents a parallel item")
    parallel_item_index: int | None = Field(default=None, description="Parallel item index if is_parallel")


class ErrorStepLifecycle(BaseModel):
    """The step failed. Error details are bundled into lifecycle."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["error"] = Field(default="error")
    message: str = Field(..., description="Human-readable error message")
    stage: ErrorStepLifecycleStage | None = None
    category: ErrorStepLifecycleCategory | None = None
    details: ErrorDetails | None = None


class PendingStepLifecycle(BaseModel):
    """The step has been created but execution has not started."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["pending"] = Field(default="pending")


class PublicHandlePayload(BaseModel):
    """Public handle payload exposed by workflow step APIs."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: PublicHandlePayloadType = Field(..., description="Type of payload")
    document: FileRef | None = Field(default=None, description="For file handles: document reference")
    data: Any | None = Field(default=None, description="For JSON handles: structured data")


class QueuedStepLifecycle(BaseModel):
    """The step is queued for execution."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["queued"] = Field(default="queued")


class RunningStepLifecycle(BaseModel):
    """The step is currently executing."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["running"] = Field(default="running")


class SkippedStepLifecycle(BaseModel):
    """The step was skipped."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["skipped"] = Field(default="skipped")
    reason: str = Field(..., description="Reason the step was skipped")


class WorkflowRunStep(BaseModel):
    """Public step status object."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    block_id: str = Field(..., description="Logical ID of the block")
    step_id: str = Field(..., description="Full step ID with iteration context. Assigned ONCE at creation, never recomputed.")
    block_type: WorkflowRunStepBlockType = Field(..., description="Type of the block")
    block_label: str = Field(..., description="Label of the block")
    lifecycle: PendingStepLifecycle | QueuedStepLifecycle | RunningStepLifecycle | CompletedStepLifecycle | AwaitingReviewStepLifecycle | ErrorStepLifecycle | SkippedStepLifecycle | CancelledStepLifecycle = Field(..., description="Current step lifecycle", discriminator="status")
    started_at: datetime.datetime | None = Field(default=None, description="When the step started executing")
    completed_at: datetime.datetime | None = Field(default=None, description="When the step finished executing")
    model: str | None = Field(default=None, description="LLM model used by this step, when applicable")
    loop_containers: list[ContainerContextData] | None = Field(default=[], description="Container hierarchy from outermost to innermost. Empty when not inside any container.")
    run_id: str = Field(..., description="Parent workflow run ID")
    created_at: datetime.datetime | None = Field(default=None, description="When the step was created")
    handle_inputs: dict[str, PublicHandlePayload] | None = Field(default={}, description="Handle input payloads consumed by this step")
    handle_outputs: dict[str, PublicHandlePayload] | None = Field(default={}, description="Handle output payloads produced by this step")
    artifact: StepArtifactRef | None = Field(default=None, description="Reference to the result produced by this step, if any.")
    retry_count: int | None = Field(default=0, description="Number of retry attempts")


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
AwaitingReviewStepLifecycle.model_rebuild()
CancelledStepLifecycle.model_rebuild()
CompletedStepLifecycle.model_rebuild()
ContainerContextData.model_rebuild()
ErrorStepLifecycle.model_rebuild()
PendingStepLifecycle.model_rebuild()
PublicHandlePayload.model_rebuild()
QueuedStepLifecycle.model_rebuild()
RunningStepLifecycle.model_rebuild()
SkippedStepLifecycle.model_rebuild()
WorkflowRunStep.model_rebuild()
