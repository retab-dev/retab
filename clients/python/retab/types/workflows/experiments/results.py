# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from enum import Enum
from typing import Literal
from pydantic import BaseModel, ConfigDict, Field
from retab.types.workflows.artifacts import ErrorDetails
from retab.types.workflows.blocks.executions import StepArtifactRef
from retab.types.workflows.experiments import FileHandleInput, JsonHandleInput


class ExperimentResultBlockType(str, Enum):
    EXTRACT = "extract"
    CLASSIFIER = "classifier"
    SPLIT = "split"
    FOR_EACH = "for_each"


class CancelledWorkflowExperimentResult(BaseModel):
    """The result row was cancelled (typically propagated from a cancelled run)."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["cancelled"] = Field(default="cancelled")
    reason: str | None = Field(default=None, description="Human-readable reason, when known")


class CompletedWorkflowExperimentResult(BaseModel):
    """The result row finished successfully."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["completed"] = Field(default="completed")


class ErrorWorkflowExperimentResult(BaseModel):
    """The result row failed. Per-job error message is bundled into lifecycle."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["error"] = Field(default="error")
    message: str | None = Field(default="(no message)", description="Human-readable error message")
    details: ErrorDetails | None = Field(default=None, description="Structured error context including stack trace")


class ExperimentResultTiming(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    created_at: datetime.datetime | None = None
    started_at: datetime.datetime | None = None
    completed_at: datetime.datetime | None = None
    duration_ms: int | None = None


class PendingWorkflowExperimentResult(BaseModel):
    """The result row has been created but execution has not started."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["pending"] = Field(default="pending")


class QueuedWorkflowExperimentResult(BaseModel):
    """The result row is enqueued and waiting for a worker."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["queued"] = Field(default="queued")


class RunningWorkflowExperimentResult(BaseModel):
    """The result row is executing."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["running"] = Field(default="running")


class ExperimentResult(BaseModel):
    """Run-scoped per-document experiment result.

    The storage row is still named ``experiment_jobs`` internally, but the
    public contract is a result row addressed by ``run_id`` + ``document_id``."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    run_id: str
    experiment_id: str
    document_id: str
    lifecycle: (
        PendingWorkflowExperimentResult
        | QueuedWorkflowExperimentResult
        | RunningWorkflowExperimentResult
        | CompletedWorkflowExperimentResult
        | ErrorWorkflowExperimentResult
        | CancelledWorkflowExperimentResult
    ) = Field(..., discriminator="status")
    timing: ExperimentResultTiming
    block_type: ExperimentResultBlockType
    handle_inputs: dict[str, JsonHandleInput | FileHandleInput] | None = Field(default={})
    artifact: StepArtifactRef | None = None
    attempt: int | None = Field(default=0)


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
CancelledWorkflowExperimentResult.model_rebuild()
CompletedWorkflowExperimentResult.model_rebuild()
ErrorWorkflowExperimentResult.model_rebuild()
ExperimentResultTiming.model_rebuild()
PendingWorkflowExperimentResult.model_rebuild()
QueuedWorkflowExperimentResult.model_rebuild()
RunningWorkflowExperimentResult.model_rebuild()
ExperimentResult.model_rebuild()
