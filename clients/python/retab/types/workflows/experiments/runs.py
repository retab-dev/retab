# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from enum import Enum, IntEnum
from typing import Literal
from pydantic import BaseModel, ConfigDict, Field
from retab.types.workflows.artifacts import ErrorDetails


class ExperimentRunBlockType(str, Enum):
    EXTRACT = "extract"
    CLASSIFIER = "classifier"
    SPLIT = "split"
    FOR_EACH = "for_each"


class ExperimentRunNConsensus(IntEnum):
    N_3 = 3
    N_5 = 5
    N_7 = 7


class CancelWorkflowExperimentRunResponse(BaseModel):
    """Result of cancelling an experiment run: the run `id` and its resulting `lifecycle` state."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    lifecycle: PendingWorkflowExperimentRun | QueuedWorkflowExperimentRun | RunningWorkflowExperimentRun | CompletedWorkflowExperimentRun | ErrorWorkflowExperimentRun | CancelledWorkflowExperimentRun = Field(..., discriminator="status")


class CancelledWorkflowExperimentRun(BaseModel):
    """The experiment run was cancelled before reaching a natural terminal state."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["cancelled"] = Field(default="cancelled")
    reason: str | None = Field(default=None, description="Human-readable reason, when known")


class CompletedWorkflowExperimentRun(BaseModel):
    """The experiment run finished successfully."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["completed"] = Field(default="completed")


class CreateExperimentRunRequest(BaseModel):
    """Request body to create an experiment run.

`workflow_id` is optional; when omitted it is taken from the experiment,
and when supplied it must match the experiment's workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    experiment_id: str = Field(..., description="The experiment to create a run for.")
    workflow_id: str | None = Field(default=None, description="Optional. When omitted, the workflow is derived from the experiment record. When supplied, must match the experiment's workflow_id (404 otherwise).")


class ErrorWorkflowExperimentRun(BaseModel):
    """The experiment run failed.

Carries a human-readable `message` and a structured `details` envelope
consumers can branch on instead of parsing free text."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["error"] = Field(default="error")
    message: str | None = Field(default="(no message)", description="Human-readable error message")
    details: ErrorDetails | None = Field(default=None, description="Structured error context including stack trace")


class ExperimentRunTiming(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    created_at: datetime.datetime | None = Field(default=None, description="When the experiment run record was created")
    started_at: datetime.datetime | None = None
    completed_at: datetime.datetime | None = None
    duration_ms: int | None = None


class ExperimentRunTrigger(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: str | None = None


class PendingWorkflowExperimentRun(BaseModel):
    """The experiment run has been created but execution has not started."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["pending"] = Field(default="pending")


class QueuedWorkflowExperimentRun(BaseModel):
    """The experiment run is enqueued and waiting for a worker."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["queued"] = Field(default="queued")


class RunningWorkflowExperimentRun(BaseModel):
    """The experiment run is executing."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: Literal["running"] = Field(default="running")


class ExperimentRun(BaseModel):
    """A single execution of an experiment, identified by `id`."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    workflow_id: str
    workflow_version_id: str
    trigger: ExperimentRunTrigger
    experiment_id: str
    block_id: str
    block_type: ExperimentRunBlockType
    n_consensus: ExperimentRunNConsensus
    lifecycle: PendingWorkflowExperimentRun | QueuedWorkflowExperimentRun | RunningWorkflowExperimentRun | CompletedWorkflowExperimentRun | ErrorWorkflowExperimentRun | CancelledWorkflowExperimentRun = Field(..., discriminator="status")
    timing: ExperimentRunTiming
    parent_run_id: str | None = None
    block_version_id: str | None = None
    metrics_validity_fingerprint: str | None = None
    metrics_validity_fingerprint_version: int | None = None
    definition_fingerprint: str
    documents_fingerprint: str
    score: float | None = None
    total_document_count: int | None = Field(default=0)
    completed_document_count: int | None = Field(default=0)
    document_count: int | None = Field(default=0)
    error_count: int | None = Field(default=0)


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
CancelWorkflowExperimentRunResponse.model_rebuild()
CancelledWorkflowExperimentRun.model_rebuild()
CompletedWorkflowExperimentRun.model_rebuild()
CreateExperimentRunRequest.model_rebuild()
ErrorWorkflowExperimentRun.model_rebuild()
ExperimentRunTiming.model_rebuild()
ExperimentRunTrigger.model_rebuild()
PendingWorkflowExperimentRun.model_rebuild()
QueuedWorkflowExperimentRun.model_rebuild()
RunningWorkflowExperimentRun.model_rebuild()
ExperimentRun.model_rebuild()
