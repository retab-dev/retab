# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from enum import Enum
from typing import Any, Literal
from pydantic import BaseModel, ConfigDict, Field


class ExperimentSummaryMetricsResponseBlockType(str, Enum):
    EXTRACT = "extract"
    CLASSIFIER = "classifier"
    SPLIT = "split"
    FOR_EACH = "for_each"


class ExperimentRunMetricsView(str, Enum):
    SUMMARY = "summary"
    BY_DOCUMENT = "by_document"
    BY_TARGET = "by_target"
    VOTES = "votes"


class ExperimentByDocumentMetricsResponse(BaseModel):
    """One document's compact per-target breakdown."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    run_id: str
    kind: Literal["by_document"] = Field(default="by_document")
    view: Literal["by_document"] = Field(default="by_document")
    document: ExperimentMetricDocumentRef
    score: float | None = None
    prior_score: float | None = None
    confusion: ExperimentDocumentConfusionMetric | None = None
    targets: list[ExperimentByDocumentTargetMetric] | None = Field(default=[])


class ExperimentByDocumentTargetMetric(BaseModel):
    """Compact target score returned by the per-document metrics view."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    path: str
    score: float | None = None
    prior_score: float | None = None
    value: Any | None = None


class ExperimentByTargetDocumentMetric(BaseModel):
    """Compact document score returned by the per-target metrics view."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    filename: str
    score: float | None = None
    prior_score: float | None = None
    value: Any | None = None


class ExperimentByTargetMetricsResponse(BaseModel):
    """One target's compact per-document distribution."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    run_id: str
    kind: Literal["by_target"] = Field(default="by_target")
    view: Literal["by_target"] = Field(default="by_target")
    target: str
    score: float | None = None
    prior_score: float | None = None
    confusion: ExperimentTargetConfusionMetric | None = None
    documents: list[ExperimentByTargetDocumentMetric] | None = Field(default=[])


class ExperimentConfusionFlowMetric(BaseModel):
    """One non-diagonal confusion flow between two labels."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    source: str
    target: str
    score: float


class ExperimentConfusionSummaryAggregate(BaseModel):
    """Split/classifier diagnostics attached to the summary response."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    diag: dict[str, float] | None = Field(default={})
    flows: list[ExperimentConfusionFlowMetric] | None = Field(default=[])


class ExperimentDocumentConfusionMetric(BaseModel):
    """Document-local confusion summary for split/classifier blocks."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    diag: dict[str, float] | None = Field(default={})
    flows: list[ExperimentConfusionFlowMetric] | None = Field(default=[])


class ExperimentExtractSummaryAggregate(BaseModel):
    """Extract-only diagnostics attached to the summary response."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    likelihoods: dict[str, float] | None = Field(default={})


class ExperimentMetricDocumentRef(BaseModel):
    """Document identity used by compact metric responses."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    filename: str


class ExperimentMetricsMissingError(BaseModel):
    """Returned when the experiment has no runs at all."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    kind: Literal["no_metrics"] = Field(default="no_metrics")
    error: Literal["no_metrics"] = Field(default="no_metrics")
    experiment_id: str
    message: str


class ExperimentMetricsStaleError(BaseModel):
    """Returned when the latest run's config or document set has drifted
    from the current draft, so its metrics no longer reflect the
    experiment's definition."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    kind: Literal["stale_metrics"] = Field(default="stale_metrics")
    error: Literal["stale_metrics"] = Field(default="stale_metrics")
    experiment_id: str
    stale_reasons: list[str] | None = Field(default=[])
    last_run: _MetricsStaleErrorLastRun
    current_config_fingerprint: str | None = None
    message: str


class ExperimentSummaryMetricDocument(BaseModel):
    """Lightweight document row returned by the summary view."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    filename: str
    score: float | None = None
    prior_score: float | None = None


class ExperimentSummaryMetricsResponse(BaseModel):
    """Run-level summary plus block-specific diagnostics.

    ``prior_run_id`` + ``prior_score`` populate when the request opts into
    prior-comparison and a completed prior run exists."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    experiment_id: str
    run_id: str
    kind: Literal["summary"] = Field(default="summary")
    view: Literal["summary"] = Field(default="summary")
    definition_fingerprint: str | None = None
    block_type: ExperimentSummaryMetricsResponseBlockType
    score: float | None = None
    prior_score: float | None = None
    documents: list[ExperimentSummaryMetricDocument] | None = Field(default=[])
    aggregate: ExperimentExtractSummaryAggregate | ExperimentConfusionSummaryAggregate | None = None
    prior_run_id: str | None = None


class ExperimentTargetConfusionMetric(BaseModel):
    """Directional confusion slice for one split/classifier target."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    self: float | None = None
    flow_from: dict[str, float] | None = Field(default={})
    flow_to: dict[str, float] | None = Field(default={})


class ExperimentVoteRow(BaseModel):
    """Consensus and flat voter values for one selected target row."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    consensus: Any | None = None
    votes: list[Any] | None = Field(default=[])
    score: float | None = None
    row_presence_score: float | None = None
    present_voter_count: int | None = None
    total_voter_count: int | None = None


class ExperimentVotesMetricDocument(BaseModel):
    """Document identity for the vote matrix response."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    filename: str


class ExperimentVotesMetricsResponse(BaseModel):
    """One document/target vote matrix with flat consensus rows."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    run_id: str
    kind: Literal["votes"] = Field(default="votes")
    view: Literal["votes"] = Field(default="votes")
    document: ExperimentVotesMetricDocument
    target: str
    score: float | None = None
    prior_score: float | None = None
    rows: list[ExperimentVoteRow] | None = Field(default=[])


class _MetricsStaleErrorLastRun(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    run_id: str
    definition_fingerprint: str | None = None
    score: float | None = None
    created_at: datetime.datetime | None = None


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
ExperimentByDocumentMetricsResponse.model_rebuild()
ExperimentByDocumentTargetMetric.model_rebuild()
ExperimentByTargetDocumentMetric.model_rebuild()
ExperimentByTargetMetricsResponse.model_rebuild()
ExperimentConfusionFlowMetric.model_rebuild()
ExperimentConfusionSummaryAggregate.model_rebuild()
ExperimentDocumentConfusionMetric.model_rebuild()
ExperimentExtractSummaryAggregate.model_rebuild()
ExperimentMetricDocumentRef.model_rebuild()
ExperimentMetricsMissingError.model_rebuild()
ExperimentMetricsStaleError.model_rebuild()
ExperimentSummaryMetricDocument.model_rebuild()
ExperimentSummaryMetricsResponse.model_rebuild()
ExperimentTargetConfusionMetric.model_rebuild()
ExperimentVoteRow.model_rebuild()
ExperimentVotesMetricDocument.model_rebuild()
ExperimentVotesMetricsResponse.model_rebuild()
_MetricsStaleErrorLastRun.model_rebuild()
