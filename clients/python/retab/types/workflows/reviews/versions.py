# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from typing import Any
from pydantic import BaseModel, ConfigDict, Field
from retab.types.workflows.reviews import Actor


class CreateReviewVersionRequest(BaseModel):
    """Create a corrected version of a review.

`parent_id` must reference an existing version under the same
`review_id`."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    review_id: str
    parent_id: str
    snapshot: dict[str, Any] = Field(..., description="The full reviewed snapshot to store as an immutable version. The object must match the gated block type: extract uses the raw output object; classifier uses {'category': string}; split uses {'documents': [{'name': string, 'pages': positive sorted int[]}]}; for_each uses {'partitions': [{'key': string, 'pages': positive sorted int[]}]}. The server validates the shape and stores the exact submitted object when valid.")
    note: str | None = None


class ReviewVersion(BaseModel):
    """Public API shape for one immutable review version."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    review_id: str
    parent_id: str | None = None
    author: Actor = Field(..., description="Actor that created the version.")
    snapshot: dict[str, Any]
    note: str | None = None
    created_at: datetime.datetime = Field(..., description="When the review version was created.")


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
CreateReviewVersionRequest.model_rebuild()
ReviewVersion.model_rebuild()
