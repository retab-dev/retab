# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from enum import Enum
from typing import Any
from pydantic import BaseModel, ConfigDict, Field


class WorkflowConfigBlockType(str, Enum):
    START_DOCUMENT = "start_document"
    START_JSON = "start_json"
    NOTE = "note"
    PARSE = "parse"
    EDIT = "edit"
    EXTRACT = "extract"
    SPLIT = "split"
    CLASSIFIER = "classifier"
    CONDITIONAL = "conditional"
    API_CALL = "api_call"
    REVIEW = "review"
    FUNCTION = "function"
    WHILE_LOOP = "while_loop"
    FOR_EACH = "for_each"
    MERGE_DICTS = "merge_dicts"
    WHILE_LOOP_SENTINEL_START = "while_loop_sentinel_start"
    WHILE_LOOP_SENTINEL_END = "while_loop_sentinel_end"
    FOR_EACH_SENTINEL_START = "for_each_sentinel_start"
    FOR_EACH_SENTINEL_END = "for_each_sentinel_end"


class WorkflowDiagnosisIssueSeverity(str, Enum):
    ERROR = "error"
    WARNING = "warning"
    INFO = "info"


class CreateWorkflowRequest(BaseModel):
    model_config = ConfigDict(extra="forbid", populate_by_name=True, protected_namespaces=())

    name: str | None = Field(default="Untitled Workflow", description="The name of the workflow")
    description: str | None = Field(default="", description="Description of the workflow")


class PublishWorkflowRequest(BaseModel):
    """Optional request body for publishing a workflow."""

    model_config = ConfigDict(extra="forbid", populate_by_name=True, protected_namespaces=())

    description: str | None = Field(default="", description="Optional description for this published version")


class UpdateWorkflowRequest(BaseModel):
    model_config = ConfigDict(extra="forbid", populate_by_name=True, protected_namespaces=())

    name: str | None = Field(default=None, description="The name of the workflow")
    description: str | None = Field(default=None, description="Description of the workflow")


class Workflow(BaseModel):
    """Public workflow resource returned by workflow metadata endpoints.

    This is the API response shape — distinct from the internal storage
    model (:class:`StoredWorkflow`), which carries persistence-only fields
    that never appear in API responses. Routes call
    :func:`serialize_workflow_response` to convert the storage shape into
    this response shape; constructing this class directly for persistence
    would drop those storage-only fields silently. The two classes were
    deliberately renamed to avoid the prior name collision."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str = Field(..., description="Unique ID for this workflow")
    name: str | None = Field(default="Untitled Workflow", description="The name of the workflow")
    description: str | None = Field(default="", description="Description of the workflow")
    published: WorkflowPublished | None = Field(default=None, description="Published workflow metadata when a published version exists")
    email_trigger: WorkflowEmailTrigger | None = Field(default=None, description="Email trigger allowlist policy")
    created_at: datetime.datetime
    updated_at: datetime.datetime


class WorkflowBlockPosition(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    x: float
    y: float


class WorkflowConfigBlock(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str | None = None
    type: WorkflowConfigBlockType
    position: WorkflowBlockPosition
    label: str
    config: dict[str, Any] | None = Field(
        default=None,
        description="Block-specific configuration as a free-form mapping. The wire schema is `additionalProperties: true` (no JSON Schema constraint) because the concrete shape is determined by the sibling `type` field — see `WorkflowBlock.config` for the per-type contract summary and the API reference at https://retab.com/docs/api-reference for full details. The SDK intentionally mirrors the spec's permissive shape rather than synthesizing a typed union.",
    )
    resolved_schemas: dict[str, Any] | None = Field(default=None, description="Derived schema transport sidecar for UI/runtime consumers. Not authored config.")
    width: float | None = Field(default=None, description="Block width for resizable blocks")
    height: float | None = Field(default=None, description="Block height for resizable blocks")
    parent_id: str | None = Field(default=None, description="ID of parent container block (while_loop, for_each)")


class WorkflowConfigEdge(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str | None = None
    source: str = Field(..., description="ID of the source block")
    target: str = Field(..., description="ID of the target block")
    source_handle: str | None = None
    target_handle: str | None = None
    animated: bool | None = Field(default=True)


class WorkflowDiagnosisIssue(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    severity: WorkflowDiagnosisIssueSeverity = Field(..., description="Issue severity level")
    code: str = Field(..., description="Stable issue code")
    message: str = Field(..., description="Human-readable issue description")
    block_id: str | None = Field(default=None, description="Related workflow block when applicable")


class WorkflowDiagnosisResponse(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    is_valid: bool = Field(..., description="Whether the graph has no error-severity issues or blocking structural warnings that make it non-runnable")
    issues: list[WorkflowDiagnosisIssue] | None = Field(default=None, description="Structured diagnosis issues")
    suggestions: list[str] | None = Field(default=None, description="Optional remediation suggestions")
    stats: WorkflowDiagnosisStats | None = Field(default=None, description="Workflow graph statistics")


class WorkflowDiagnosisStats(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    total_blocks: int | None = Field(default=0, description="Total number of blocks diagnosed")
    total_edges: int | None = Field(default=0, description="Total number of edges diagnosed")
    block_types: dict[str, int] | None = Field(default=None, description="Counts by block type")
    start_document_blocks: int | None = Field(default=0, description="Number of start_document blocks")


class WorkflowEmailTrigger(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    allowed_senders: list[str] | None = Field(default=None, description="Allowed sender email addresses for workflow email triggers")
    allowed_domains: list[str] | None = Field(default=None, description="Allowed sender email domains for workflow email triggers")


class WorkflowGraphDiagnosisRequest(BaseModel):
    """Request payload for ``POST /workflows/{id}/diagnose-graph``.

    ``blocks`` and ``edges`` are both nullable so the route can tell apart
    two intents:

    * ``blocks`` / ``edges`` omitted (``None``): diagnose the persisted draft
      — the route loads blocks and edges from MongoDB.
    * ``blocks`` / ``edges`` provided (even as ``[]``): diagnose the in-memory
      graph as-is — used by editor previews and ``workflows spec validate``
      against a stub file.

    Sending only one side (e.g. ``blocks=[]`` without ``edges``) is treated as
    explicit-empty for the missing side too; the persisted-draft fallback only
    kicks in when both are omitted."""

    model_config = ConfigDict(extra="forbid", populate_by_name=True, protected_namespaces=())

    blocks: list[WorkflowConfigBlock] | None = Field(default=None, description="Blocks to diagnose; if omitted, the persisted draft is loaded server-side")
    edges: list[WorkflowConfigEdge] | None = Field(default=None, description="Edges to diagnose; if omitted, the persisted draft is loaded server-side")
    re_propagate: bool | None = Field(default=True, description="Recompute derived schemas before validating the graph")


class WorkflowPublished(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    version_id: str | None = Field(default=None, description="Published content-addressed workflow version ID")
    published_at: datetime.datetime | None = Field(default=None, description="When the workflow was last published")
    description: str | None = Field(
        default="",
        description="Release note attached to the currently published version. Echoes the ``description`` body passed to ``POST /v1/workflows/{id}/publish`` so the caller can confirm it was stored without a separate fetch.",
    )


from .artifacts import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .blocks import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .edges import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .experiments import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .reviews import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .runs import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .specs import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .steps import *  # noqa: F401,F403  (re-export sub-resource symbols)

from .tests import *  # noqa: F401,F403  (re-export sub-resource symbols)

__all__ = [
    "Actor",
    "ActorKind",
    "AllItemsMatchCondition",
    "AnyItemMatchesCondition",
    "ApiCallAttempt",
    "ApiCallInvocation",
    "ApiTrigger",
    "ApproveReviewRequest",
    "ArrayContainsCondition",
    "AssertionDriftStatus",
    "AssertionSchemaDep",
    "AssertionSpec",
    "AwaitingReviewRun",
    "AwaitingReviewStepLifecycle",
    "BBox",
    "BetweenCondition",
    "CancelWorkflowRequest",
    "CancelWorkflowResponse",
    "CancelWorkflowResponseCancellationStatus",
    "CancelledStepLifecycle",
    "CancelledTerminal",
    "ClassificationWorkflowArtifact",
    "CompletedStepLifecycle",
    "CompletedTerminal",
    "ConditionEvaluationDetails",
    "ConditionEvaluationDetailsLogicalOperator",
    "ConditionEvaluationPerItem",
    "ConditionEvaluationResult",
    "ConditionEvaluationResultLogicalOperator",
    "ConditionEvaluationSubCondition",
    "ConditionalEvaluation",
    "ContainCondition",
    "ContainerContextData",
    "CreateExperimentRequest",
    "CreateExperimentRequestNConsensus",
    "CreateFreshWorkflowRunRequest",
    "CreateRestartWorkflowRunRequest",
    "CreateRestartWorkflowRunRequestConfigSource",
    "CreateWorkflowRequest",
    "CreateWorkflowRunRequest",
    "CreateWorkflowTestRequest",
    "DeclarativeApplyResponse",
    "DeclarativeApplyResponseAction",
    "DeclarativeExportResponse",
    "DeclarativePlanChange",
    "DeclarativePlanFieldChange",
    "DeclarativePlanFieldChangeAction",
    "DeclarativePlanResourceChange",
    "DeclarativePlanResourceChangeActions",
    "DeclarativePlanResourceChangeTarget",
    "DeclarativePlanResourceChangeType",
    "DeclarativePlanResponse",
    "DeclarativePlanResponseAction",
    "DeclarativePlanSummary",
    "DeclarativeValidationResponse",
    "DeclarativeWorkflowRequest",
    "EditConfig",
    "EditResult",
    "EditWorkflowArtifact",
    "EmailTrigger",
    "EndsWithCondition",
    "EqualCondition",
    "ErrorDetails",
    "ErrorStepLifecycle",
    "ErrorStepLifecycleCategory",
    "ErrorStepLifecycleStage",
    "ErrorTerminal",
    "ErrorTerminalCategory",
    "ErrorTerminalStage",
    "ExistCondition",
    "ExperimentBlockType",
    "ExperimentDocumentCaptureRequest",
    "ExperimentDocumentProvenance",
    "ExperimentPublicStatus",
    "ExperimentSchemaDriftStatus",
    "ExplicitExperimentDocumentRequest",
    "ExtractionWorkflowArtifact",
    "FieldType",
    "FormField",
    "FunctionInvocation",
    "HandlePayload",
    "HandlePayloadType",
    "JsonSchemaValidCondition",
    "LatestBlockTestRunSummary",
    "LatestBlockTestRunSummaryOutcome",
    "LatestBlockTestRunSummaryStatus",
    "LengthCompareCondition",
    "LengthCompareConditionOp",
    "LlmJudgedAsCondition",
    "LlmNotJudgedAsCondition",
    "ManualTrigger",
    "MatcheRegexCondition",
    "NConsensusValue",
    "NotContainsCondition",
    "NotEqualsCondition",
    "NotExistsCondition",
    "NumberCompareCondition",
    "NumberCompareConditionOp",
    "ObjectContainsCondition",
    "OutputTarget",
    "ParseWorkflowArtifact",
    "ParseWorkflowArtifactTableParsingFormat",
    "PartitionWorkflowArtifact",
    "PendingRun",
    "PendingStepLifecycle",
    "PublishWorkflowRequest",
    "QueuedStepLifecycle",
    "RejectReviewRequest",
    "RestartTrigger",
    "ResumeStatus",
    "Review",
    "ReviewAllOf",
    "ReviewAlways",
    "ReviewAnyOf",
    "ReviewAnyRequiredFieldNull",
    "ReviewAnySplitPagesLt",
    "ReviewBlockType",
    "ReviewBoundaryConfidenceLt",
    "ReviewBranchIn",
    "ReviewCategoryIn",
    "ReviewConfidenceLt",
    "ReviewDecision",
    "ReviewDecisionStatus",
    "ReviewEvaluation",
    "ReviewEvaluationReviewDecision",
    "ReviewFieldConfidenceLt",
    "ReviewJsonCondition",
    "ReviewSplitCountNeq",
    "ReviewTopMarginLt",
    "ReviewValidationFailed",
    "ReviewVerdict",
    "RunInputs",
    "RunTiming",
    "RunningRun",
    "RunningStepLifecycle",
    "ScheduleTrigger",
    "SimilarityGteCondition",
    "SimilarityGteConditionMethod",
    "SkippedStepLifecycle",
    "SplitIouCondition",
    "SplitWorkflowArtifact",
    "StartWithCondition",
    "StepArtifactRef",
    "StepArtifactRefOperation",
    "SubmissionStatus",
    "SubmitDecisionResponse",
    "UpdateExperimentRequest",
    "UpdateExperimentRequestNConsensus",
    "UpdateWorkflowBlockRequest",
    "UpdateWorkflowBlockRequestConfigMode",
    "UpdateWorkflowRequest",
    "UpdateWorkflowTestRequest",
    "WebhookTrigger",
    "WhileLoopTermination",
    "WhileLoopTerminationTerminationReason",
    "Workflow",
    "WorkflowArtifact",
    "WorkflowArtifactOperation",
    "WorkflowBlock",
    "WorkflowBlockCreateRequest",
    "WorkflowBlockCreateRequestType",
    "WorkflowBlockPosition",
    "WorkflowBlockType",
    "WorkflowConfigBlock",
    "WorkflowConfigBlockType",
    "WorkflowConfigEdge",
    "WorkflowDiagnosisIssue",
    "WorkflowDiagnosisIssueSeverity",
    "WorkflowDiagnosisResponse",
    "WorkflowDiagnosisStats",
    "WorkflowEdgeCreateRequest",
    "WorkflowEdgeDoc",
    "WorkflowEmailTrigger",
    "WorkflowExperiment",
    "WorkflowExportPayloadRequest",
    "WorkflowExportPayloadRequestExcludeStatus",
    "WorkflowExportPayloadRequestExportSource",
    "WorkflowExportPayloadRequestStatus",
    "WorkflowExportPayloadRequestTriggerTypes",
    "WorkflowExportPayloadResponse",
    "WorkflowGraphDiagnosisRequest",
    "WorkflowPublished",
    "WorkflowRun",
    "WorkflowRunStep",
    "WorkflowRunStepBlockType",
    "WorkflowSnapshotRef",
    "WorkflowTest",
    "WorkflowTestSchemaDrift",
]


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
CreateWorkflowRequest.model_rebuild()
PublishWorkflowRequest.model_rebuild()
UpdateWorkflowRequest.model_rebuild()
Workflow.model_rebuild()
WorkflowBlockPosition.model_rebuild()
WorkflowConfigBlock.model_rebuild()
WorkflowConfigEdge.model_rebuild()
WorkflowDiagnosisIssue.model_rebuild()
WorkflowDiagnosisResponse.model_rebuild()
WorkflowDiagnosisStats.model_rebuild()
WorkflowEmailTrigger.model_rebuild()
WorkflowGraphDiagnosisRequest.model_rebuild()
WorkflowPublished.model_rebuild()
