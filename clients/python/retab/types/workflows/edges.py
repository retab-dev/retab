# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

import datetime
from pydantic import BaseModel, ConfigDict, Field


class WorkflowEdgeCreateRequest(BaseModel):
    """Body for POST /v1/workflows/edges."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    workflow_id: str = Field(..., description="Workflow to create the edge in.")
    id: str | None = Field(default=None, description="If omitted, the server generates an opaque `edg_<nanoid>`. Opaque edge ID. Omit to let the server generate one.")
    source_block: str = Field(..., description="Source block ID")
    target_block: str = Field(..., description="Target block ID")
    source_handle: str | None = Field(default=None, description="Output handle")
    target_handle: str | None = Field(default=None, description="Input handle")


class WorkflowEdgeDoc(BaseModel):
    """Public live workflow edge object."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    workflow_id: str = Field(..., description="Foreign key to workflow")
    source_block: str = Field(..., description="ID of the source block")
    target_block: str = Field(..., description="ID of the target block")
    source_handle: str | None = Field(default=None, description="Output handle on source block")
    target_handle: str | None = Field(default=None, description="Input handle on target block")
    updated_at: datetime.datetime


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
WorkflowEdgeCreateRequest.model_rebuild()
WorkflowEdgeDoc.model_rebuild()
