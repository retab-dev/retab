# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Literal
from pydantic import BaseModel, ConfigDict, Field
from retab.types.workflows.experiments.runs import WorkflowSnapshotRef
from retab.types.workflows.runs import ApiTrigger, EmailTrigger, ManualTrigger, RestartTrigger, ScheduleTrigger, WebhookTrigger
from retab.types.workflows.tests import WorkflowTestBlockTarget
from retab.types.workflows.tests.results import (
    CancelledWorkflowTestRun,
    CompletedWorkflowTestRun,
    ErrorWorkflowTestRun,
    PendingWorkflowTestRun,
    QueuedWorkflowTestRun,
    RunningWorkflowTestRun,
    WorkflowTestRunTiming,
)


class BlockTestBatchExecutionCounts(BaseModel):
    """Aggregate counts for a batch of block-test runs.

    Each individual run contributes to exactly one `lifecycle_counts`
    bucket, and additionally to one `outcome` bucket when
    `lifecycle_counts.completed` is incremented."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    lifecycle_counts: BlockTestLifecycleCounts | None = Field(default={"pending": 0, "queued": 0, "running": 0, "completed": 0, "error": 0, "cancelled": 0}, validate_default=True)
    outcome: BlockTestOutcomeCounts | None = Field(default={"passed": 0, "failed": 0, "blocked": 0}, validate_default=True)


class BlockTestLifecycleCounts(BaseModel):
    """Per-lifecycle counts for a batch of block-test runs."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    pending: int | None = Field(default=0)
    queued: int | None = Field(default=0)
    running: int | None = Field(default=0)
    completed: int | None = Field(default=0)
    error: int | None = Field(default=0)
    cancelled: int | None = Field(default=0)


class BlockTestOutcomeCounts(BaseModel):
    """Per-outcome counts. Only completed runs contribute to these buckets."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    passed: int | None = Field(default=0)
    failed: int | None = Field(default=0)
    blocked: int | None = Field(default=0)


class CreateWorkflowTestRunRequest(BaseModel):
    """Create a workflow test run. Provide a `workflow_id`, and optionally narrow execution with `scope` to a single test or one block. Omit `scope` to run every saved workflow test."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    workflow_id: str
    scope: WorkflowTestRunSingleScope | WorkflowTestRunWorkflowScope | WorkflowTestRunBlockScope | None = Field(
        default=None, description="Optional execution scope. Omit (or pass null) to run every saved test in the workflow.", discriminator="type"
    )


class WorkflowTestRun(BaseModel):
    """A batch execution of a workflow's tests, with overall `lifecycle`, `timing`, and pass/fail `counts`."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    workflow: WorkflowSnapshotRef
    trigger: ManualTrigger | ApiTrigger | ScheduleTrigger | WebhookTrigger | EmailTrigger | RestartTrigger = Field(..., discriminator="type")
    lifecycle: PendingWorkflowTestRun | QueuedWorkflowTestRun | RunningWorkflowTestRun | CompletedWorkflowTestRun | ErrorWorkflowTestRun | CancelledWorkflowTestRun = Field(
        ..., discriminator="status"
    )
    timing: WorkflowTestRunTiming
    target: WorkflowTestBlockTarget | None = None
    test_id: str | None = None
    total_tests: int
    counts: BlockTestBatchExecutionCounts | None = Field(
        default={"lifecycle_counts": {"cancelled": 0, "completed": 0, "error": 0, "pending": 0, "queued": 0, "running": 0}, "outcome": {"blocked": 0, "failed": 0, "passed": 0}},
        validate_default=True,
    )


class WorkflowTestRunBlockScope(BaseModel):
    """Run every workflow test for one block in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["block"]
    block_id: str


class WorkflowTestRunSingleScope(BaseModel):
    """Run one saved workflow test in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["single"]
    test_id: str


class WorkflowTestRunWorkflowScope(BaseModel):
    """Run every saved test in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["workflow"]


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
BlockTestBatchExecutionCounts.model_rebuild()
BlockTestLifecycleCounts.model_rebuild()
BlockTestOutcomeCounts.model_rebuild()
CreateWorkflowTestRunRequest.model_rebuild()
WorkflowTestRun.model_rebuild()
WorkflowTestRunBlockScope.model_rebuild()
WorkflowTestRunSingleScope.model_rebuild()
WorkflowTestRunWorkflowScope.model_rebuild()
