# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from enum import Enum
from typing import Literal, cast
from pydantic import BaseModel, ConfigDict, Field
from retab.types.workflows.evals.results import (
    CancelledWorkflowEvalRun,
    CompletedWorkflowEvalRun,
    ErrorWorkflowEvalRun,
    PendingWorkflowEvalRun,
    QueuedWorkflowEvalRun,
    RunningWorkflowEvalRun,
    WorkflowEvalRunTiming,
)


class EvalRunFreshnessStatus(str, Enum):
    FRESH = "fresh"
    STALE = "stale"
    UNKNOWN = "unknown"


class EvalRunFreshnessReasons(str, Enum):
    VALIDITY_CHANGED = "validity_changed"
    INPUTS_CHANGED = "inputs_changed"
    ENGINE_CHANGED = "engine_changed"
    METRICS_ENGINE_CHANGED = "metrics_engine_changed"
    NO_BASELINE = "no_baseline"


class EvalRunTriggerType(str, Enum):
    MANUAL = "manual"
    API = "api"
    SCHEDULE = "schedule"
    WEBHOOK = "webhook"
    EMAIL = "email"
    CUSTOM = "custom"
    RESTART = "restart"


class BlockEvalBatchExecutionCounts(BaseModel):
    """Aggregate counts for a batch of block-eval runs.

    Each individual run contributes to exactly one `lifecycle_counts`
    bucket, and additionally to one `outcome` bucket when
    `lifecycle_counts.completed` is incremented."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    lifecycle_counts: BlockEvalLifecycleCounts | None = Field(default={"pending": 0, "queued": 0, "running": 0, "completed": 0, "error": 0, "cancelled": 0}, validate_default=True)
    outcome: BlockEvalOutcomeCounts | None = Field(default={"passed": 0, "failed": 0, "blocked": 0}, validate_default=True)


class BlockEvalLifecycleCounts(BaseModel):
    """Per-lifecycle counts for a batch of block-eval runs."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    pending: int | None = Field(default=0)
    queued: int | None = Field(default=0)
    running: int | None = Field(default=0)
    completed: int | None = Field(default=0)
    error: int | None = Field(default=0)
    cancelled: int | None = Field(default=0)


class BlockEvalOutcomeCounts(BaseModel):
    """Per-outcome counts. Only completed runs contribute to these buckets."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    passed: int | None = Field(default=0)
    failed: int | None = Field(default=0)
    blocked: int | None = Field(default=0)


class CreateWorkflowEvalRunRequest(BaseModel):
    """Create a workflow eval run. Provide a `workflow_id`, and optionally narrow execution with `scope` to a single eval or one block. Omit `scope` to run every saved workflow eval."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    workflow_id: str
    scope: WorkflowEvalRunSingleScope | WorkflowEvalRunWorkflowScope | WorkflowEvalRunBlockScope | None = Field(
        default=None, description="Optional execution scope. Omit (or pass null) to run every saved eval in the workflow.", discriminator="type"
    )


class EvalRunBlockTarget(BaseModel):
    """Public workflow-eval target.

    The storage layer remains block-scoped today, but the API shape names the
    tested entity explicitly so workflow-level targets can be added later."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["block"] = Field(default="block")
    block_id: str


class EvalRunFreshness(BaseModel):
    """Compatibility envelope on WorkflowEvalRun. This is not the authoritative stale/fresh verdict for saved eval definitions; use WorkflowEval.freshness for current staleness presentation."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    status: EvalRunFreshnessStatus | None = Field(default=cast(EvalRunFreshnessStatus, "unknown"), validate_default=True)
    reasons: list[EvalRunFreshnessReasons] | None = Field(default=[])
    validity_fingerprint: str | None
    input_fingerprint: str | None
    baseline_run_id: str | None


class EvalRunTrigger(BaseModel):
    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: EvalRunTriggerType


class WorkflowEvalRun(BaseModel):
    """A batch execution of a workflow's evals, with overall `lifecycle`, `timing`, and pass/fail `counts`."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    id: str
    workflow_id: str
    workflow_version_id: str
    trigger: EvalRunTrigger
    lifecycle: PendingWorkflowEvalRun | QueuedWorkflowEvalRun | RunningWorkflowEvalRun | CompletedWorkflowEvalRun | ErrorWorkflowEvalRun | CancelledWorkflowEvalRun = Field(
        ..., discriminator="status"
    )
    timing: WorkflowEvalRunTiming
    target: EvalRunBlockTarget | None = None
    eval_id: str | None = None
    total_evals: int
    counts: BlockEvalBatchExecutionCounts | None = Field(
        default={"lifecycle_counts": {"cancelled": 0, "completed": 0, "error": 0, "pending": 0, "queued": 0, "running": 0}, "outcome": {"blocked": 0, "failed": 0, "passed": 0}},
        validate_default=True,
    )
    freshness: EvalRunFreshness | None = Field(
        default=None, description="Compatibility envelope only. WorkflowEval.freshness is the authoritative read-time staleness verdict for saved eval definitions."
    )


class WorkflowEvalRunBlockScope(BaseModel):
    """Run every workflow eval for one block in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["block"]
    block_id: str


class WorkflowEvalRunSingleScope(BaseModel):
    """Run one saved workflow eval in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["single"]
    eval_id: str


class WorkflowEvalRunWorkflowScope(BaseModel):
    """Run every saved eval in the workflow."""

    model_config = ConfigDict(extra="ignore", populate_by_name=True, protected_namespaces=())

    type: Literal["workflow"]


# Resolve forward references (Pydantic v2). Safe no-op when
# the model is already fully built; needed when annotations
# are lazily evaluated strings under `from __future__ import
# annotations` and a referenced symbol comes from another
# generated module via a TYPE_CHECKING-guarded import.
BlockEvalBatchExecutionCounts.model_rebuild()
BlockEvalLifecycleCounts.model_rebuild()
BlockEvalOutcomeCounts.model_rebuild()
CreateWorkflowEvalRunRequest.model_rebuild()
EvalRunBlockTarget.model_rebuild()
EvalRunFreshness.model_rebuild()
EvalRunTrigger.model_rebuild()
WorkflowEvalRun.model_rebuild()
WorkflowEvalRunBlockScope.model_rebuild()
WorkflowEvalRunSingleScope.model_rebuild()
WorkflowEvalRunWorkflowScope.model_rebuild()
