# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Any
from io import IOBase
from pathlib import Path
import PIL.Image
from pydantic import HttpUrl

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.utils.mime import prepare_mime_document
from retab.types.mime import FileRef, MIMEData
from retab.types.parses import Parse, ParseRequest, ParseRequestTableParsingFormat


def _coerce_mime_document_input(document: Path | str | bytes | IOBase | FileRef | MIMEData | PIL.Image.Image | HttpUrl) -> FileRef | dict[str, Any]:
    if isinstance(document, FileRef):
        return document
    mime_data = prepare_mime_document(document)
    return {
        "filename": mime_data.filename,
        "url": mime_data.url,
        "mime_type": mime_data.mime_type,
    }


class ParsesMixin:
    def prepare_list(
        self,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 10,
        order: PaginationOrder | None = "desc",
        filename: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Parses"""
        params: dict[str, Any] = {
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
            "filename": filename,
            "from_date": from_date,
            "to_date": to_date,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/parses", params=params or None, data=data)

    def prepare_create(
        self,
        document: Path | str | bytes | IOBase | FileRef | MIMEData | PIL.Image.Image | HttpUrl,
        model: str = "retab-small",
        table_parsing_format: ParseRequestTableParsingFormat = "html",
        image_resolution_dpi: int = 192,
        instructions: str | None = None,
        bust_cache: bool = False,
        **extra_params: Any,
    ) -> PreparedRequest:
        """Create Parse"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        if document is not None:
            document = _coerce_mime_document_input(document)
        payload = ParseRequest(
            document=document, model=model, table_parsing_format=table_parsing_format, image_resolution_dpi=image_resolution_dpi, instructions=instructions, bust_cache=bust_cache
        )
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="POST", url="/v1/parses", params=params or None, data=data)

    def prepare_get(self, parse_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Parse"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/parses/{parse_id}", params=params or None, data=data)

    def prepare_delete(self, parse_id: str, **extra_params: Any) -> PreparedRequest:
        """Delete Parse"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="DELETE", url=f"/v1/parses/{parse_id}", params=params or None, data=data)


class Parses(SyncAPIResource, ParsesMixin):
    """Parses API wrapper."""

    def list(
        self,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 10,
        order: PaginationOrder | None = "desc",
        filename: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PaginatedList[Parse]:
        """List Parses"""
        request = self.prepare_list(before=before, after=after, limit=limit, order=order, filename=filename, from_date=from_date, to_date=to_date, **extra_params)
        return self.request_page(request, model=Parse)

    def create(
        self,
        document: Path | str | bytes | IOBase | FileRef | MIMEData | PIL.Image.Image | HttpUrl,
        model: str = "retab-small",
        table_parsing_format: ParseRequestTableParsingFormat = "html",
        image_resolution_dpi: int = 192,
        instructions: str | None = None,
        bust_cache: bool = False,
        **extra_params: Any,
    ) -> Parse:
        """Create Parse"""
        request = self.prepare_create(
            document=document,
            model=model,
            table_parsing_format=table_parsing_format,
            image_resolution_dpi=image_resolution_dpi,
            instructions=instructions,
            bust_cache=bust_cache,
            **extra_params,
        )
        response = self._client._prepared_request(request)
        return Parse.model_validate(response)

    def get(self, parse_id: str, **extra_params: Any) -> Parse:
        """Get Parse"""
        request = self.prepare_get(parse_id, **extra_params)
        response = self._client._prepared_request(request)
        return Parse.model_validate(response)

    def delete(self, parse_id: str, **extra_params: Any) -> None:
        """Delete Parse"""
        request = self.prepare_delete(parse_id, **extra_params)
        self._client._prepared_request(request)
        return None


class AsyncParses(AsyncAPIResource, ParsesMixin):
    """Async Parses API wrapper."""

    async def list(
        self,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 10,
        order: PaginationOrder | None = "desc",
        filename: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> AsyncPaginatedList[Parse]:
        """List Parses"""
        request = self.prepare_list(before=before, after=after, limit=limit, order=order, filename=filename, from_date=from_date, to_date=to_date, **extra_params)
        return await self.request_page(request, model=Parse)

    async def create(
        self,
        document: Path | str | bytes | IOBase | FileRef | MIMEData | PIL.Image.Image | HttpUrl,
        model: str = "retab-small",
        table_parsing_format: ParseRequestTableParsingFormat = "html",
        image_resolution_dpi: int = 192,
        instructions: str | None = None,
        bust_cache: bool = False,
        **extra_params: Any,
    ) -> Parse:
        """Create Parse"""
        request = self.prepare_create(
            document=document,
            model=model,
            table_parsing_format=table_parsing_format,
            image_resolution_dpi=image_resolution_dpi,
            instructions=instructions,
            bust_cache=bust_cache,
            **extra_params,
        )
        response = await self._client._prepared_request(request)
        return Parse.model_validate(response)

    async def get(self, parse_id: str, **extra_params: Any) -> Parse:
        """Get Parse"""
        request = self.prepare_get(parse_id, **extra_params)
        response = await self._client._prepared_request(request)
        return Parse.model_validate(response)

    async def delete(self, parse_id: str, **extra_params: Any) -> None:
        """Delete Parse"""
        request = self.prepare_delete(parse_id, **extra_params)
        await self._client._prepared_request(request)
        return None


__all__ = ["Parses", "AsyncParses", "ParsesMixin"]
