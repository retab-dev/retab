# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations
# ruff: noqa: F401

from typing import Any, cast

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.usage import UsageBlockRecord, UsagePrimitiveRecord, UsageRunRecord


class UsageMixin:
    _client: Any

    def prepare_list_blocks(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        block_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Usage Blocks"""
        params: dict[str, Any] = {
            "limit": limit,
            "order": order,
            "before": before,
            "after": after,
            "workflow_id": workflow_id,
            "block_type": block_type,
            "from_date": from_date,
            "to_date": to_date,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/usage/blocks", params=params or None, data=data)

    def prepare_list_primitives(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        environment_id: str | None = None,
        workflow_id: str | None = None,
        project_id: str | None = None,
        run_id: str | None = None,
        block_id: str | None = None,
        operation: str | None = None,
        status: str | None = None,
        metadata: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Usage Primitives"""
        params: dict[str, Any] = {
            "limit": limit,
            "order": order,
            "before": before,
            "after": after,
            "environment_id": environment_id,
            "workflow_id": workflow_id,
            "project_id": project_id,
            "run_id": run_id,
            "block_id": block_id,
            "operation": operation,
            "status": status,
            "metadata": metadata,
            "from_date": from_date,
            "to_date": to_date,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/usage/primitives", params=params or None, data=data)

    def prepare_list_runs(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        status: str | None = None,
        trigger_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Usage Runs"""
        params: dict[str, Any] = {
            "limit": limit,
            "order": order,
            "before": before,
            "after": after,
            "workflow_id": workflow_id,
            "status": status,
            "trigger_type": trigger_type,
            "from_date": from_date,
            "to_date": to_date,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/usage/runs", params=params or None, data=data)


class Usage(SyncAPIResource, UsageMixin):
    """Usage API wrapper."""

    def list_blocks(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        block_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PaginatedList[UsageBlockRecord]:
        """List Usage Blocks"""
        prepared_request = self.prepare_list_blocks(
            limit=limit, order=order, before=before, after=after, workflow_id=workflow_id, block_type=block_type, from_date=from_date, to_date=to_date, **extra_params
        )
        return self.request_page(prepared_request, model=UsageBlockRecord)

    def list_primitives(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        environment_id: str | None = None,
        workflow_id: str | None = None,
        project_id: str | None = None,
        run_id: str | None = None,
        block_id: str | None = None,
        operation: str | None = None,
        status: str | None = None,
        metadata: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PaginatedList[UsagePrimitiveRecord]:
        """List Usage Primitives"""
        prepared_request = self.prepare_list_primitives(
            limit=limit,
            order=order,
            before=before,
            after=after,
            environment_id=environment_id,
            workflow_id=workflow_id,
            project_id=project_id,
            run_id=run_id,
            block_id=block_id,
            operation=operation,
            status=status,
            metadata=metadata,
            from_date=from_date,
            to_date=to_date,
            **extra_params,
        )
        return self.request_page(prepared_request, model=UsagePrimitiveRecord)

    def list_runs(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        status: str | None = None,
        trigger_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> PaginatedList[UsageRunRecord]:
        """List Usage Runs"""
        prepared_request = self.prepare_list_runs(
            limit=limit,
            order=order,
            before=before,
            after=after,
            workflow_id=workflow_id,
            status=status,
            trigger_type=trigger_type,
            from_date=from_date,
            to_date=to_date,
            **extra_params,
        )
        return self.request_page(prepared_request, model=UsageRunRecord)


class AsyncUsage(AsyncAPIResource, UsageMixin):
    """Async Usage API wrapper."""

    async def list_blocks(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        block_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> AsyncPaginatedList[UsageBlockRecord]:
        """List Usage Blocks"""
        prepared_request = self.prepare_list_blocks(
            limit=limit, order=order, before=before, after=after, workflow_id=workflow_id, block_type=block_type, from_date=from_date, to_date=to_date, **extra_params
        )
        return await self.request_page(prepared_request, model=UsageBlockRecord)

    async def list_primitives(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        environment_id: str | None = None,
        workflow_id: str | None = None,
        project_id: str | None = None,
        run_id: str | None = None,
        block_id: str | None = None,
        operation: str | None = None,
        status: str | None = None,
        metadata: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> AsyncPaginatedList[UsagePrimitiveRecord]:
        """List Usage Primitives"""
        prepared_request = self.prepare_list_primitives(
            limit=limit,
            order=order,
            before=before,
            after=after,
            environment_id=environment_id,
            workflow_id=workflow_id,
            project_id=project_id,
            run_id=run_id,
            block_id=block_id,
            operation=operation,
            status=status,
            metadata=metadata,
            from_date=from_date,
            to_date=to_date,
            **extra_params,
        )
        return await self.request_page(prepared_request, model=UsagePrimitiveRecord)

    async def list_runs(
        self,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        before: str | None = None,
        after: str | None = None,
        workflow_id: str | None = None,
        status: str | None = None,
        trigger_type: str | None = None,
        from_date: str | None = None,
        to_date: str | None = None,
        **extra_params: Any,
    ) -> AsyncPaginatedList[UsageRunRecord]:
        """List Usage Runs"""
        prepared_request = self.prepare_list_runs(
            limit=limit,
            order=order,
            before=before,
            after=after,
            workflow_id=workflow_id,
            status=status,
            trigger_type=trigger_type,
            from_date=from_date,
            to_date=to_date,
            **extra_params,
        )
        return await self.request_page(prepared_request, model=UsageRunRecord)


__all__ = ["Usage", "AsyncUsage", "UsageMixin"]
