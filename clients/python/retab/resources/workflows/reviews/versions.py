# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Any

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList
from retab.types.workflows.reviews.versions import CreateReviewVersionRequest, ReviewVersion


class WorkflowReviewVersionsMixin:
    def prepare_list(self, review_id: str, before: str | None = None, after: str | None = None, limit: int | None = 50, **extra_params: Any) -> PreparedRequest:
        """List Review Versions Route List versions for one review. ``review_id`` is required by design — listing versions across all reviews has no product use and would expose a needlessly wide query surface."""
        params: dict[str, Any] = {
            "review_id": review_id,
            "before": before,
            "after": after,
            "limit": limit,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/reviews/versions", params=params or None, data=data)

    def prepare_create(self, review_id: str, parent_id: str, snapshot: dict[str, Any], note: str | None = None, **extra_params: Any) -> PreparedRequest:
        """Create Review Version Route Create one immutable, content-addressed review version."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        payload = CreateReviewVersionRequest(review_id=review_id, parent_id=parent_id, snapshot=snapshot, note=note)
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="POST", url="/v1/workflows/reviews/versions", params=params or None, data=data)

    def prepare_get(self, version_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Review Version Route Read one review version by its content-addressed id."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/reviews/versions/{version_id}", params=params or None, data=data)


class WorkflowReviewVersions(SyncAPIResource, WorkflowReviewVersionsMixin):
    """WorkflowReviewVersions API wrapper."""

    def list(self, review_id: str, before: str | None = None, after: str | None = None, limit: int | None = 50, **extra_params: Any) -> PaginatedList[ReviewVersion]:
        """List Review Versions Route List versions for one review. ``review_id`` is required by design — listing versions across all reviews has no product use and would expose a needlessly wide query surface."""
        request = self.prepare_list(review_id=review_id, before=before, after=after, limit=limit, **extra_params)
        return self.request_page(request, model=ReviewVersion)

    def create(self, review_id: str, parent_id: str, snapshot: dict[str, Any], note: str | None = None, **extra_params: Any) -> ReviewVersion:
        """Create Review Version Route Create one immutable, content-addressed review version."""
        request = self.prepare_create(review_id=review_id, parent_id=parent_id, snapshot=snapshot, note=note, **extra_params)
        response = self._client._prepared_request(request)
        return ReviewVersion.model_validate(response)

    def get(self, version_id: str, **extra_params: Any) -> ReviewVersion:
        """Get Review Version Route Read one review version by its content-addressed id."""
        request = self.prepare_get(version_id, **extra_params)
        response = self._client._prepared_request(request)
        return ReviewVersion.model_validate(response)


class AsyncWorkflowReviewVersions(AsyncAPIResource, WorkflowReviewVersionsMixin):
    """Async WorkflowReviewVersions API wrapper."""

    async def list(self, review_id: str, before: str | None = None, after: str | None = None, limit: int | None = 50, **extra_params: Any) -> AsyncPaginatedList[ReviewVersion]:
        """List Review Versions Route List versions for one review. ``review_id`` is required by design — listing versions across all reviews has no product use and would expose a needlessly wide query surface."""
        request = self.prepare_list(review_id=review_id, before=before, after=after, limit=limit, **extra_params)
        return await self.request_page(request, model=ReviewVersion)

    async def create(self, review_id: str, parent_id: str, snapshot: dict[str, Any], note: str | None = None, **extra_params: Any) -> ReviewVersion:
        """Create Review Version Route Create one immutable, content-addressed review version."""
        request = self.prepare_create(review_id=review_id, parent_id=parent_id, snapshot=snapshot, note=note, **extra_params)
        response = await self._client._prepared_request(request)
        return ReviewVersion.model_validate(response)

    async def get(self, version_id: str, **extra_params: Any) -> ReviewVersion:
        """Get Review Version Route Read one review version by its content-addressed id."""
        request = self.prepare_get(version_id, **extra_params)
        response = await self._client._prepared_request(request)
        return ReviewVersion.model_validate(response)


__all__ = ["WorkflowReviewVersions", "AsyncWorkflowReviewVersions", "WorkflowReviewVersionsMixin"]
