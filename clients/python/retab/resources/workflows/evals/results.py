# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations
# ruff: noqa: F401

from typing import Any, cast

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.workflows.evals.results import WorkflowEvalResult


class WorkflowEvalRunResultsMixin:
    _client: Any

    def prepare_list(
        self,
        run_id: str,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Workflow Eval Results List workflow eval results for a single run, page by page. Results are returned in run-time order."""
        params: dict[str, Any] = {
            "run_id": run_id,
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/evals/results", params=params or None, data=data)

    def prepare_get(self, result_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Workflow Eval Result Retrieve a single workflow eval result. Identified by `result_id`. Returns the result for one eval within a run, including its `verdict` (`passed`, `failed`, or `blocked`), lifecycle, timing, and any error. Returns 404 if no result with that ID exists."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/evals/results/{result_id}", params=params or None, data=data)


class WorkflowEvalRunResults(SyncAPIResource, WorkflowEvalRunResultsMixin):
    """WorkflowEvalRunResults API wrapper."""

    def list(
        self,
        run_id: str,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PaginatedList[WorkflowEvalResult]:
        """List Workflow Eval Results List workflow eval results for a single run, page by page. Results are returned in run-time order."""
        prepared_request = self.prepare_list(run_id=run_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return self.request_page(prepared_request, model=WorkflowEvalResult)

    def get(self, result_id: str, **extra_params: Any) -> WorkflowEvalResult:
        """Get Workflow Eval Result Retrieve a single workflow eval result. Identified by `result_id`. Returns the result for one eval within a run, including its `verdict` (`passed`, `failed`, or `blocked`), lifecycle, timing, and any error. Returns 404 if no result with that ID exists."""
        prepared_request = self.prepare_get(result_id, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowEvalResult.model_validate(response)


class AsyncWorkflowEvalRunResults(AsyncAPIResource, WorkflowEvalRunResultsMixin):
    """Async WorkflowEvalRunResults API wrapper."""

    async def list(
        self,
        run_id: str,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 20,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> AsyncPaginatedList[WorkflowEvalResult]:
        """List Workflow Eval Results List workflow eval results for a single run, page by page. Results are returned in run-time order."""
        prepared_request = self.prepare_list(run_id=run_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return await self.request_page(prepared_request, model=WorkflowEvalResult)

    async def get(self, result_id: str, **extra_params: Any) -> WorkflowEvalResult:
        """Get Workflow Eval Result Retrieve a single workflow eval result. Identified by `result_id`. Returns the result for one eval within a run, including its `verdict` (`passed`, `failed`, or `blocked`), lifecycle, timing, and any error. Returns 404 if no result with that ID exists."""
        prepared_request = self.prepare_get(result_id, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowEvalResult.model_validate(response)


__all__ = ["WorkflowEvalRunResults", "AsyncWorkflowEvalRunResults", "WorkflowEvalRunResultsMixin"]
