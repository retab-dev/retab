# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations
# ruff: noqa: F401

from typing import Any, cast
import datetime

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.workflows.evals.runs import CreateWorkflowEvalRunRequest, WorkflowEvalRun, WorkflowEvalRunBlockScope, WorkflowEvalRunSingleScope, WorkflowEvalRunWorkflowScope


class WorkflowEvalRunsMixin:
    _client: Any

    def prepare_list(
        self,
        workflow_id: str | None = None,
        eval_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = cast(str, "created_at"),
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Workflow Eval Runs List workflow eval runs. Optionally filter by `workflow_id`, `eval_id`, `target_block_id`, `status`/`exclude_status`, `trigger_type`, and a `from_date`/`to_date` window. Returns a cursor-paginated list ordered by `sort_by` (default newest first)."""
        params: dict[str, Any] = {
            "workflow_id": workflow_id,
            "eval_id": eval_id,
            "target_block_id": target_block_id,
            "status": status,
            "exclude_status": exclude_status,
            "trigger_type": trigger_type,
            "from_date": from_date,
            "to_date": to_date,
            "sort_by": sort_by,
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/evals/runs", params=params or None, data=data)

    def prepare_create(
        self, workflow_id: str, scope: WorkflowEvalRunSingleScope | WorkflowEvalRunWorkflowScope | WorkflowEvalRunBlockScope | None = None, **extra_params: Any
    ) -> PreparedRequest:
        """Create Workflow Eval Run Create a workflow-scoped eval run. `workflow_id` is the execution context. Optional `scope` narrows the run to one saved eval or one block; omitted scope runs all workflow evals."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        payload = CreateWorkflowEvalRunRequest(workflow_id=cast(Any, workflow_id), scope=cast(Any, scope))
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="POST", url="/v1/workflows/evals/runs", params=params or None, data=data)

    def prepare_get(self, run_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Workflow Eval Run Retrieve a single workflow eval run. Identified by `run_id`. Returns the run with its lifecycle status, timing, and pass/fail counts. Returns 404 if no run with that ID exists."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/evals/runs/{run_id}", params=params or None, data=data)

    def prepare_cancel(self, run_id: str, **extra_params: Any) -> PreparedRequest:
        """Cancel Workflow Eval Run Cancel a workflow eval run. Identified by `run_id`. Stops the run and returns it with its updated cancelled lifecycle. Returns 404 if the run does not exist or is not in a cancellable state."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="POST", url=f"/v1/workflows/evals/runs/{run_id}/cancel", params=params or None, data=data)


class WorkflowEvalRuns(SyncAPIResource, WorkflowEvalRunsMixin):
    """WorkflowEvalRuns API wrapper."""

    def list(
        self,
        workflow_id: str | None = None,
        eval_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = cast(str, "created_at"),
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PaginatedList[WorkflowEvalRun]:
        """List Workflow Eval Runs List workflow eval runs. Optionally filter by `workflow_id`, `eval_id`, `target_block_id`, `status`/`exclude_status`, `trigger_type`, and a `from_date`/`to_date` window. Returns a cursor-paginated list ordered by `sort_by` (default newest first)."""
        prepared_request = self.prepare_list(
            workflow_id=workflow_id,
            eval_id=eval_id,
            target_block_id=target_block_id,
            status=status,
            exclude_status=exclude_status,
            trigger_type=trigger_type,
            from_date=from_date,
            to_date=to_date,
            sort_by=sort_by,
            before=before,
            after=after,
            limit=limit,
            order=order,
            **extra_params,
        )
        return self.request_page(prepared_request, model=WorkflowEvalRun)

    def create(
        self, workflow_id: str, scope: WorkflowEvalRunSingleScope | WorkflowEvalRunWorkflowScope | WorkflowEvalRunBlockScope | None = None, **extra_params: Any
    ) -> WorkflowEvalRun:
        """Create Workflow Eval Run Create a workflow-scoped eval run. `workflow_id` is the execution context. Optional `scope` narrows the run to one saved eval or one block; omitted scope runs all workflow evals."""
        prepared_request = self.prepare_create(workflow_id=workflow_id, scope=scope, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)

    def get(self, run_id: str, **extra_params: Any) -> WorkflowEvalRun:
        """Get Workflow Eval Run Retrieve a single workflow eval run. Identified by `run_id`. Returns the run with its lifecycle status, timing, and pass/fail counts. Returns 404 if no run with that ID exists."""
        prepared_request = self.prepare_get(run_id, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)

    def cancel(self, run_id: str, **extra_params: Any) -> WorkflowEvalRun:
        """Cancel Workflow Eval Run Cancel a workflow eval run. Identified by `run_id`. Stops the run and returns it with its updated cancelled lifecycle. Returns 404 if the run does not exist or is not in a cancellable state."""
        prepared_request = self.prepare_cancel(run_id, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)


class AsyncWorkflowEvalRuns(AsyncAPIResource, WorkflowEvalRunsMixin):
    """Async WorkflowEvalRuns API wrapper."""

    async def list(
        self,
        workflow_id: str | None = None,
        eval_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = cast(str, "created_at"),
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> AsyncPaginatedList[WorkflowEvalRun]:
        """List Workflow Eval Runs List workflow eval runs. Optionally filter by `workflow_id`, `eval_id`, `target_block_id`, `status`/`exclude_status`, `trigger_type`, and a `from_date`/`to_date` window. Returns a cursor-paginated list ordered by `sort_by` (default newest first)."""
        prepared_request = self.prepare_list(
            workflow_id=workflow_id,
            eval_id=eval_id,
            target_block_id=target_block_id,
            status=status,
            exclude_status=exclude_status,
            trigger_type=trigger_type,
            from_date=from_date,
            to_date=to_date,
            sort_by=sort_by,
            before=before,
            after=after,
            limit=limit,
            order=order,
            **extra_params,
        )
        return await self.request_page(prepared_request, model=WorkflowEvalRun)

    async def create(
        self, workflow_id: str, scope: WorkflowEvalRunSingleScope | WorkflowEvalRunWorkflowScope | WorkflowEvalRunBlockScope | None = None, **extra_params: Any
    ) -> WorkflowEvalRun:
        """Create Workflow Eval Run Create a workflow-scoped eval run. `workflow_id` is the execution context. Optional `scope` narrows the run to one saved eval or one block; omitted scope runs all workflow evals."""
        prepared_request = self.prepare_create(workflow_id=workflow_id, scope=scope, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)

    async def get(self, run_id: str, **extra_params: Any) -> WorkflowEvalRun:
        """Get Workflow Eval Run Retrieve a single workflow eval run. Identified by `run_id`. Returns the run with its lifecycle status, timing, and pass/fail counts. Returns 404 if no run with that ID exists."""
        prepared_request = self.prepare_get(run_id, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)

    async def cancel(self, run_id: str, **extra_params: Any) -> WorkflowEvalRun:
        """Cancel Workflow Eval Run Cancel a workflow eval run. Identified by `run_id`. Stops the run and returns it with its updated cancelled lifecycle. Returns 404 if the run does not exist or is not in a cancellable state."""
        prepared_request = self.prepare_cancel(run_id, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowEvalRun.model_validate(response)


__all__ = ["WorkflowEvalRuns", "AsyncWorkflowEvalRuns", "WorkflowEvalRunsMixin"]
