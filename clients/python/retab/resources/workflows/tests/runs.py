# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Any
import datetime

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.workflows.tests.runs import (
    CreateWorkflowTestRunAllRequest,
    CreateWorkflowTestRunForTargetRequest,
    CreateWorkflowTestRunForTestRequest,
    CreateWorkflowTestRunRequest,
    WorkflowTestBlockTarget,
    WorkflowTestRun,
)


class WorkflowTestRunsMixin:
    def prepare_list(
        self,
        workflow_id: str | None = None,
        test_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        statuses: list[str] | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        trigger_types: list[str] | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = "created_at",
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = "desc",
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Test Execution Runs"""
        params: dict[str, Any] = {
            "workflow_id": workflow_id,
            "test_id": test_id,
            "target_block_id": target_block_id,
            "status": status,
            "statuses": statuses,
            "exclude_status": exclude_status,
            "trigger_type": trigger_type,
            "trigger_types": trigger_types,
            "from_date": from_date,
            "to_date": to_date,
            "sort_by": sort_by,
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/tests/runs", params=params or None, data=data)

    def prepare_create(
        self, test_id: str | None = None, workflow_id: str | None = None, n_consensus: int | None = None, target: WorkflowTestBlockTarget | None = None, **extra_params: Any
    ) -> PreparedRequest:
        """Create Test Run Create a workflow-scoped test run. Scoping identity comes from the body — ``workflow_id`` and/or ``test_id`` per ``CreateWorkflowTestRunRequest``."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        if test_id is not None:
            payload = CreateWorkflowTestRunForTestRequest(test_id=test_id, workflow_id=workflow_id, n_consensus=n_consensus)
        elif target is not None:
            payload = CreateWorkflowTestRunForTargetRequest(workflow_id=workflow_id, target=target, n_consensus=n_consensus)
        else:
            payload = CreateWorkflowTestRunAllRequest(workflow_id=workflow_id, n_consensus=n_consensus)
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="POST", url="/v1/workflows/tests/runs", params=params or None, data=data)

    def prepare_get(self, run_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Test Execution Run"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/tests/runs/{run_id}", params=params or None, data=data)

    def prepare_cancel(self, run_id: str, **extra_params: Any) -> PreparedRequest:
        """Cancel Test Execution Run"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="POST", url=f"/v1/workflows/tests/runs/{run_id}/cancel", params=params or None, data=data)


class WorkflowTestRuns(SyncAPIResource, WorkflowTestRunsMixin):
    """WorkflowTestRuns API wrapper."""

    def list(
        self,
        workflow_id: str | None = None,
        test_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        statuses: list[str] | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        trigger_types: list[str] | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = "created_at",
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = "desc",
        **extra_params: Any,
    ) -> PaginatedList[WorkflowTestRun]:
        """List Test Execution Runs"""
        request = self.prepare_list(
            workflow_id=workflow_id,
            test_id=test_id,
            target_block_id=target_block_id,
            status=status,
            statuses=statuses,
            exclude_status=exclude_status,
            trigger_type=trigger_type,
            trigger_types=trigger_types,
            from_date=from_date,
            to_date=to_date,
            sort_by=sort_by,
            before=before,
            after=after,
            limit=limit,
            order=order,
            **extra_params,
        )
        return self.request_page(request, model=WorkflowTestRun)

    def create(
        self, test_id: str | None = None, workflow_id: str | None = None, n_consensus: int | None = None, target: WorkflowTestBlockTarget | None = None, **extra_params: Any
    ) -> WorkflowTestRun:
        """Create Test Run Create a workflow-scoped test run. Scoping identity comes from the body — ``workflow_id`` and/or ``test_id`` per ``CreateWorkflowTestRunRequest``."""
        request = self.prepare_create(test_id=test_id, workflow_id=workflow_id, n_consensus=n_consensus, target=target, **extra_params)
        response = self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)

    def get(self, run_id: str, **extra_params: Any) -> WorkflowTestRun:
        """Get Test Execution Run"""
        request = self.prepare_get(run_id, **extra_params)
        response = self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)

    def cancel(self, run_id: str, **extra_params: Any) -> WorkflowTestRun:
        """Cancel Test Execution Run"""
        request = self.prepare_cancel(run_id, **extra_params)
        response = self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)


class AsyncWorkflowTestRuns(AsyncAPIResource, WorkflowTestRunsMixin):
    """Async WorkflowTestRuns API wrapper."""

    async def list(
        self,
        workflow_id: str | None = None,
        test_id: str | None = None,
        target_block_id: str | None = None,
        status: str | None = None,
        statuses: list[str] | None = None,
        exclude_status: str | None = None,
        trigger_type: str | None = None,
        trigger_types: list[str] | None = None,
        from_date: datetime.datetime | None = None,
        to_date: datetime.datetime | None = None,
        sort_by: str | None = "created_at",
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = "desc",
        **extra_params: Any,
    ) -> AsyncPaginatedList[WorkflowTestRun]:
        """List Test Execution Runs"""
        request = self.prepare_list(
            workflow_id=workflow_id,
            test_id=test_id,
            target_block_id=target_block_id,
            status=status,
            statuses=statuses,
            exclude_status=exclude_status,
            trigger_type=trigger_type,
            trigger_types=trigger_types,
            from_date=from_date,
            to_date=to_date,
            sort_by=sort_by,
            before=before,
            after=after,
            limit=limit,
            order=order,
            **extra_params,
        )
        return await self.request_page(request, model=WorkflowTestRun)

    async def create(
        self, test_id: str | None = None, workflow_id: str | None = None, n_consensus: int | None = None, target: WorkflowTestBlockTarget | None = None, **extra_params: Any
    ) -> WorkflowTestRun:
        """Create Test Run Create a workflow-scoped test run. Scoping identity comes from the body — ``workflow_id`` and/or ``test_id`` per ``CreateWorkflowTestRunRequest``."""
        request = self.prepare_create(test_id=test_id, workflow_id=workflow_id, n_consensus=n_consensus, target=target, **extra_params)
        response = await self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)

    async def get(self, run_id: str, **extra_params: Any) -> WorkflowTestRun:
        """Get Test Execution Run"""
        request = self.prepare_get(run_id, **extra_params)
        response = await self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)

    async def cancel(self, run_id: str, **extra_params: Any) -> WorkflowTestRun:
        """Cancel Test Execution Run"""
        request = self.prepare_cancel(run_id, **extra_params)
        response = await self._client._prepared_request(request)
        return WorkflowTestRun.model_validate(response)


__all__ = ["WorkflowTestRuns", "AsyncWorkflowTestRuns", "WorkflowTestRunsMixin"]
