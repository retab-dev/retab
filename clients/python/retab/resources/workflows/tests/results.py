# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
from __future__ import annotations

from typing import Any

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.workflows.tests.results import WorkflowTestResult


class WorkflowTestRunResultsMixin:
    def prepare_list(
        self, run_id: str, before: str | None = None, after: str | None = None, limit: int | None = 20, order: PaginationOrder | None = "desc", **extra_params: Any
    ) -> PreparedRequest:
        """List Test Execution Results List workflow test results for a single run, page by page. Pagination strategy: the parent ``workflow_test_runs.result_run_record_ids`` document already holds the ordered list of child record IDs. ``workflow_block_test_runs`` rows do not carry a ``run_id`` field (the relationship lives only on the parent), so a direct keyset query on the child collection is not…"""
        params: dict[str, Any] = {
            "run_id": run_id,
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/tests/results", params=params or None, data=data)

    def prepare_get(self, result_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Test Execution Result"""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/tests/results/{result_id}", params=params or None, data=data)


class WorkflowTestRunResults(SyncAPIResource, WorkflowTestRunResultsMixin):
    """WorkflowTestRunResults API wrapper."""

    def list(
        self, run_id: str, before: str | None = None, after: str | None = None, limit: int | None = 20, order: PaginationOrder | None = "desc", **extra_params: Any
    ) -> PaginatedList[WorkflowTestResult]:
        """List Test Execution Results List workflow test results for a single run, page by page. Pagination strategy: the parent ``workflow_test_runs.result_run_record_ids`` document already holds the ordered list of child record IDs. ``workflow_block_test_runs`` rows do not carry a ``run_id`` field (the relationship lives only on the parent), so a direct keyset query on the child collection is not…"""
        request = self.prepare_list(run_id=run_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return self.request_page(request, model=WorkflowTestResult)

    def get(self, result_id: str, **extra_params: Any) -> WorkflowTestResult:
        """Get Test Execution Result"""
        request = self.prepare_get(result_id, **extra_params)
        response = self._client._prepared_request(request)
        return WorkflowTestResult.model_validate(response)


class AsyncWorkflowTestRunResults(AsyncAPIResource, WorkflowTestRunResultsMixin):
    """Async WorkflowTestRunResults API wrapper."""

    async def list(
        self, run_id: str, before: str | None = None, after: str | None = None, limit: int | None = 20, order: PaginationOrder | None = "desc", **extra_params: Any
    ) -> AsyncPaginatedList[WorkflowTestResult]:
        """List Test Execution Results List workflow test results for a single run, page by page. Pagination strategy: the parent ``workflow_test_runs.result_run_record_ids`` document already holds the ordered list of child record IDs. ``workflow_block_test_runs`` rows do not carry a ``run_id`` field (the relationship lives only on the parent), so a direct keyset query on the child collection is not…"""
        request = self.prepare_list(run_id=run_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return await self.request_page(request, model=WorkflowTestResult)

    async def get(self, result_id: str, **extra_params: Any) -> WorkflowTestResult:
        """Get Test Execution Result"""
        request = self.prepare_get(result_id, **extra_params)
        response = await self._client._prepared_request(request)
        return WorkflowTestResult.model_validate(response)


__all__ = ["WorkflowTestRunResults", "AsyncWorkflowTestRunResults", "WorkflowTestRunResultsMixin"]
