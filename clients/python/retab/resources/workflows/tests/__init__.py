# This file is auto-generated by the Retab oagen emitter. Do not edit by hand.
# ruff: noqa: F405
from __future__ import annotations
# ruff: noqa: F401

from typing import Any, cast

from retab._resource import AsyncAPIResource, SyncAPIResource
from retab.types.standards import PreparedRequest
from retab.types.pagination import AsyncPaginatedList, PaginatedList, PaginationOrder
from retab.types.workflows.tests import (
    AssertionSpec,
    CreateWorkflowTestRequest,
    ManualWorkflowTestSource,
    RunStepWorkflowTestSource,
    UpdateWorkflowTestRequest,
    WorkflowTest,
    WorkflowTestBlockTarget,
)

from .results import WorkflowTestRunResults, AsyncWorkflowTestRunResults
from .runs import WorkflowTestRuns, AsyncWorkflowTestRuns


class WorkflowTestsMixin:
    def prepare_list(
        self,
        workflow_id: str,
        target_block_id: str | None = None,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PreparedRequest:
        """List Tests List workflow tests. Requires `workflow_id` and returns its saved tests as a cursor-paginated list, each with its latest-run summaries and drift status. Optionally filter to one block with `target_block_id`. Returns 404 if the workflow does not exist."""
        params: dict[str, Any] = {
            "workflow_id": workflow_id,
            "target_block_id": target_block_id,
            "before": before,
            "after": after,
            "limit": limit,
            "order": order,
        }
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url="/v1/workflows/tests", params=params or None, data=data)

    def prepare_create(
        self,
        workflow_id: str,
        target: WorkflowTestBlockTarget,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource,
        assertion: AssertionSpec,
        name: str | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """Create Test Create a workflow test. Pins an expected outcome for one block in a workflow. Provide the `workflow_id`, the `target` block, an `assertion` describing the expected output, and a `source` of test inputs (explicit handle inputs or a capture from a prior run/step). Returns the created test with status 201."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        payload = CreateWorkflowTestRequest(
            workflow_id=cast(Any, workflow_id), target=cast(Any, target), source=cast(Any, source), name=cast(Any, name), assertion=cast(Any, assertion)
        )
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="POST", url="/v1/workflows/tests", params=params or None, data=data)

    def prepare_get(self, test_id: str, **extra_params: Any) -> PreparedRequest:
        """Get Test Retrieve a single workflow test. Identified by `test_id`. Returns the test with its target block, assertion, input source, and latest-run summaries. Returns 404 if no test with that ID exists."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="GET", url=f"/v1/workflows/tests/{test_id}", params=params or None, data=data)

    def prepare_update(
        self,
        test_id: str,
        name: str | None = None,
        assertion: AssertionSpec | None = None,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource | None = None,
        **extra_params: Any,
    ) -> PreparedRequest:
        """Update Test Update a workflow test. Identified by `test_id`. Send any of `name`, `assertion`, or `source`; omitted fields are left unchanged. Returns the updated test."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        payload = UpdateWorkflowTestRequest(name=cast(Any, name), assertion=cast(Any, assertion), source=cast(Any, source))
        data = payload.model_dump(mode="json", exclude_none=True, by_alias=True) if payload is not None else None
        return PreparedRequest(method="PATCH", url=f"/v1/workflows/tests/{test_id}", params=params or None, data=data)

    def prepare_delete(self, test_id: str, **extra_params: Any) -> PreparedRequest:
        """Delete Test Delete a workflow test. Identified by `test_id`. Returns 204 on success and 404 if no test with that ID exists."""
        params: dict[str, Any] = {}
        if extra_params:
            params.update(extra_params)
        params = {k: v for k, v in params.items() if v is not None}
        data = None
        return PreparedRequest(method="DELETE", url=f"/v1/workflows/tests/{test_id}", params=params or None, data=data)


class WorkflowTests(SyncAPIResource, WorkflowTestsMixin):
    """WorkflowTests API wrapper."""

    def __init__(self, client: Any) -> None:
        super().__init__(client=client)
        self.results = WorkflowTestRunResults(client=client)
        self.runs = WorkflowTestRuns(client=client)

    def list(
        self,
        workflow_id: str,
        target_block_id: str | None = None,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> PaginatedList[WorkflowTest]:
        """List Tests List workflow tests. Requires `workflow_id` and returns its saved tests as a cursor-paginated list, each with its latest-run summaries and drift status. Optionally filter to one block with `target_block_id`. Returns 404 if the workflow does not exist."""
        prepared_request = self.prepare_list(workflow_id=workflow_id, target_block_id=target_block_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return self.request_page(prepared_request, model=WorkflowTest)

    def create(
        self,
        workflow_id: str,
        target: WorkflowTestBlockTarget,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource,
        assertion: AssertionSpec,
        name: str | None = None,
        **extra_params: Any,
    ) -> WorkflowTest:
        """Create Test Create a workflow test. Pins an expected outcome for one block in a workflow. Provide the `workflow_id`, the `target` block, an `assertion` describing the expected output, and a `source` of test inputs (explicit handle inputs or a capture from a prior run/step). Returns the created test with status 201."""
        prepared_request = self.prepare_create(workflow_id=workflow_id, target=target, source=source, name=name, assertion=assertion, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    def get(self, test_id: str, **extra_params: Any) -> WorkflowTest:
        """Get Test Retrieve a single workflow test. Identified by `test_id`. Returns the test with its target block, assertion, input source, and latest-run summaries. Returns 404 if no test with that ID exists."""
        prepared_request = self.prepare_get(test_id, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    def update(
        self,
        test_id: str,
        name: str | None = None,
        assertion: AssertionSpec | None = None,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource | None = None,
        **extra_params: Any,
    ) -> WorkflowTest:
        """Update Test Update a workflow test. Identified by `test_id`. Send any of `name`, `assertion`, or `source`; omitted fields are left unchanged. Returns the updated test."""
        prepared_request = self.prepare_update(test_id, name=name, assertion=assertion, source=source, **extra_params)
        response = self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    def delete(self, test_id: str, **extra_params: Any) -> None:
        """Delete Test Delete a workflow test. Identified by `test_id`. Returns 204 on success and 404 if no test with that ID exists."""
        prepared_request = self.prepare_delete(test_id, **extra_params)
        self._client._prepared_request(prepared_request)
        return None


class AsyncWorkflowTests(AsyncAPIResource, WorkflowTestsMixin):
    """Async WorkflowTests API wrapper."""

    def __init__(self, client: Any) -> None:
        super().__init__(client=client)
        self.results = AsyncWorkflowTestRunResults(client=client)
        self.runs = AsyncWorkflowTestRuns(client=client)

    async def list(
        self,
        workflow_id: str,
        target_block_id: str | None = None,
        before: str | None = None,
        after: str | None = None,
        limit: int | None = 50,
        order: PaginationOrder | None = cast(PaginationOrder, "desc"),
        **extra_params: Any,
    ) -> AsyncPaginatedList[WorkflowTest]:
        """List Tests List workflow tests. Requires `workflow_id` and returns its saved tests as a cursor-paginated list, each with its latest-run summaries and drift status. Optionally filter to one block with `target_block_id`. Returns 404 if the workflow does not exist."""
        prepared_request = self.prepare_list(workflow_id=workflow_id, target_block_id=target_block_id, before=before, after=after, limit=limit, order=order, **extra_params)
        return await self.request_page(prepared_request, model=WorkflowTest)

    async def create(
        self,
        workflow_id: str,
        target: WorkflowTestBlockTarget,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource,
        assertion: AssertionSpec,
        name: str | None = None,
        **extra_params: Any,
    ) -> WorkflowTest:
        """Create Test Create a workflow test. Pins an expected outcome for one block in a workflow. Provide the `workflow_id`, the `target` block, an `assertion` describing the expected output, and a `source` of test inputs (explicit handle inputs or a capture from a prior run/step). Returns the created test with status 201."""
        prepared_request = self.prepare_create(workflow_id=workflow_id, target=target, source=source, name=name, assertion=assertion, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    async def get(self, test_id: str, **extra_params: Any) -> WorkflowTest:
        """Get Test Retrieve a single workflow test. Identified by `test_id`. Returns the test with its target block, assertion, input source, and latest-run summaries. Returns 404 if no test with that ID exists."""
        prepared_request = self.prepare_get(test_id, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    async def update(
        self,
        test_id: str,
        name: str | None = None,
        assertion: AssertionSpec | None = None,
        source: ManualWorkflowTestSource | RunStepWorkflowTestSource | None = None,
        **extra_params: Any,
    ) -> WorkflowTest:
        """Update Test Update a workflow test. Identified by `test_id`. Send any of `name`, `assertion`, or `source`; omitted fields are left unchanged. Returns the updated test."""
        prepared_request = self.prepare_update(test_id, name=name, assertion=assertion, source=source, **extra_params)
        response = await self._client._prepared_request(prepared_request)
        return WorkflowTest.model_validate(response)

    async def delete(self, test_id: str, **extra_params: Any) -> None:
        """Delete Test Delete a workflow test. Identified by `test_id`. Returns 204 on success and 404 if no test with that ID exists."""
        prepared_request = self.prepare_delete(test_id, **extra_params)
        await self._client._prepared_request(prepared_request)
        return None


from .results import *  # noqa: E402,F401,F403  (sub-resource + grandchildren)
from .runs import *  # noqa: E402,F401,F403  (sub-resource + grandchildren)

__all__ = [
    "WorkflowTests",
    "AsyncWorkflowTests",
    "WorkflowTestsMixin",
    "WorkflowTestRunResults",
    "AsyncWorkflowTestRunResults",
    "WorkflowTestRunResultsMixin",
    "WorkflowTestRuns",
    "AsyncWorkflowTestRuns",
    "WorkflowTestRunsMixin",
]
