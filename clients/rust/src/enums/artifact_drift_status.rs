// Code generated by oagen. DO NOT EDIT.

use serde::{Deserialize, Serialize};
use std::fmt;
use std::str::FromStr;

#[derive(Debug, Clone, PartialEq, Eq, Hash)]
#[non_exhaustive]
pub enum ArtifactDriftStatus {
    None,
    Drifted,
    Broken,
    Unknown,
    /// Wire value not recognized by this SDK version. The original
    /// string is preserved verbatim. WorkOS may add new enum values
    /// server-side; matching on this variant lets callers handle
    /// forward-compatible values without panicking.
    Unrecognized(String),
}

impl ArtifactDriftStatus {
    /// Canonical wire string for this value. For [`Self::Unrecognized`] returns the
    /// original wire value as received from the API.
    #[allow(deprecated)]
    pub fn as_str(&self) -> &str {
        match self {
            Self::None => "none",
            Self::Drifted => "drifted",
            Self::Broken => "broken",
            Self::Unknown => "unknown",
            Self::Unrecognized(s) => s.as_str(),
        }
    }
}

impl fmt::Display for ArtifactDriftStatus {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        f.write_str(self.as_str())
    }
}

impl AsRef<str> for ArtifactDriftStatus {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}

impl FromStr for ArtifactDriftStatus {
    type Err = std::convert::Infallible;
    #[allow(deprecated)]
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Ok(match s {
            "none" => Self::None,
            "drifted" => Self::Drifted,
            "broken" => Self::Broken,
            "unknown" => Self::Unknown,
            other => Self::Unrecognized(other.to_string()),
        })
    }
}

impl From<String> for ArtifactDriftStatus {
    fn from(s: String) -> Self {
        // Reuse the original `String` allocation in the fallback branch.
        match Self::from_str(&s) {
            Ok(Self::Unrecognized(_)) => Self::Unrecognized(s),
            Ok(other) => other,
        }
    }
}

impl From<&str> for ArtifactDriftStatus {
    fn from(s: &str) -> Self {
        Self::from_str(s).unwrap_or_else(|_| Self::Unrecognized(s.to_string()))
    }
}

impl Serialize for ArtifactDriftStatus {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

impl<'de> Deserialize<'de> for ArtifactDriftStatus {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = String::deserialize(deserializer)?;
        Ok(Self::from(s))
    }
}
