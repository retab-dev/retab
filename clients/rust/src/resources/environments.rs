// Code generated by oagen. DO NOT EDIT.

use crate::client::Retab;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
#[allow(unused_imports)]
use serde::Serialize;

pub struct EnvironmentsApi<'a> {
    pub(crate) client: &'a Retab,
}

#[derive(Debug, Clone, Serialize)]
pub struct CreateEnvironmentParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: EnvironmentCreateRequest,
}

impl CreateEnvironmentParams {
    /// Construct a new `CreateEnvironmentParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: EnvironmentCreateRequest) -> Self {
        Self { body }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct UpdateEnvironmentParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: EnvironmentUpdateRequest,
}

impl UpdateEnvironmentParams {
    /// Construct a new `UpdateEnvironmentParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: EnvironmentUpdateRequest) -> Self {
        Self { body }
    }
}

impl<'a> EnvironmentsApi<'a> {
    /// List Organization Environments
    pub async fn list_environments(&self) -> Result<EnvironmentListResponse, Error> {
        self.list_environments_with_options(None).await
    }

    /// Variant of [`Self::list_environments`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_environments_with_options(
        &self,
        options: Option<&crate::RequestOptions>,
    ) -> Result<EnvironmentListResponse, Error> {
        let path = "/v1/environments".to_string();
        let method = http::Method::GET;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }

    /// Create Organization Environment
    pub async fn create_environment(
        &self,
        params: CreateEnvironmentParams,
    ) -> Result<EnvironmentResponse, Error> {
        self.create_environment_with_options(params, None).await
    }

    /// Variant of [`Self::create_environment`] that accepts per-request [`crate::RequestOptions`].
    pub async fn create_environment_with_options(
        &self,
        params: CreateEnvironmentParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<EnvironmentResponse, Error> {
        let path = "/v1/environments".to_string();
        let method = http::Method::POST;
        self.client
            .request_with_body_opts(method, &path, &params, Some(&params.body), options)
            .await
    }

    /// Get Organization Environment
    pub async fn get_environment(
        &self,
        environment_id: &str,
    ) -> Result<EnvironmentResponse, Error> {
        self.get_environment_with_options(environment_id, None)
            .await
    }

    /// Variant of [`Self::get_environment`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_environment_with_options(
        &self,
        environment_id: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<EnvironmentResponse, Error> {
        let environment_id = crate::client::path_segment(environment_id);
        let path = format!("/v1/environments/{environment_id}");
        let method = http::Method::GET;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }

    /// Update Organization Environment
    pub async fn update_environment(
        &self,
        environment_id: &str,
        params: UpdateEnvironmentParams,
    ) -> Result<EnvironmentResponse, Error> {
        self.update_environment_with_options(environment_id, params, None)
            .await
    }

    /// Variant of [`Self::update_environment`] that accepts per-request [`crate::RequestOptions`].
    pub async fn update_environment_with_options(
        &self,
        environment_id: &str,
        params: UpdateEnvironmentParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<EnvironmentResponse, Error> {
        let environment_id = crate::client::path_segment(environment_id);
        let path = format!("/v1/environments/{environment_id}");
        let method = http::Method::PATCH;
        self.client
            .request_with_body_opts(method, &path, &params, Some(&params.body), options)
            .await
    }

    /// Archive Organization Environment
    pub async fn delete_environment(
        &self,
        environment_id: &str,
    ) -> Result<EnvironmentResponse, Error> {
        self.delete_environment_with_options(environment_id, None)
            .await
    }

    /// Variant of [`Self::delete_environment`] that accepts per-request [`crate::RequestOptions`].
    pub async fn delete_environment_with_options(
        &self,
        environment_id: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<EnvironmentResponse, Error> {
        let environment_id = crate::client::path_segment(environment_id);
        let path = format!("/v1/environments/{environment_id}");
        let method = http::Method::DELETE;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }
}
