// Code generated by oagen. DO NOT EDIT.

use crate::client::Retab;
#[allow(unused_imports)]
use crate::enums::*;
use crate::error::Error;
#[allow(unused_imports)]
use crate::models::*;
use serde::Serialize;

pub struct ExperimentRunsApi<'a> {
    pub(crate) client: &'a Retab,
}

#[derive(Debug, Clone, Serialize)]
pub struct ListParams {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub workflow_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub experiment_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub block_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub status: Option<WorkflowExperimentsStatus>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub statuses: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub exclude_status: Option<WorkflowExperimentsExcludeStatus>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub trigger_type: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub trigger_types: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub from_date: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub to_date: Option<String>,
    /// Defaults to `created_at`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub sort_by: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub before: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub after: Option<String>,
    /// Defaults to `20`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Defaults to `desc`.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub order: Option<ExperimentRunsOrder>,
}

impl Default for ListParams {
    #[allow(deprecated)]
    fn default() -> Self {
        Self {
            workflow_id: Default::default(),
            experiment_id: Default::default(),
            block_id: Default::default(),
            status: Default::default(),
            statuses: Default::default(),
            exclude_status: Default::default(),
            trigger_type: Default::default(),
            trigger_types: Default::default(),
            from_date: Default::default(),
            to_date: Default::default(),
            sort_by: Some("created_at".to_string()),
            before: Default::default(),
            after: Default::default(),
            limit: Some(20),
            order: Some(ExperimentRunsOrder::Desc),
        }
    }
}

#[derive(Debug, Clone, Serialize)]
pub struct CreateParams {
    /// Request body sent with this call.
    ///
    /// Required.
    #[serde(skip)]
    pub body: CreateExperimentRunRequest,
}

impl CreateParams {
    /// Construct a new `CreateParams` with the required fields set.
    #[allow(deprecated)]
    pub fn new(body: CreateExperimentRunRequest) -> Self {
        Self { body }
    }
}

impl<'a> ExperimentRunsApi<'a> {
    /// List Experiment Runs
    pub async fn list(&self, params: ListParams) -> Result<WorkflowExperimentRunList, Error> {
        self.list_with_options(params, None).await
    }

    /// Variant of [`Self::list`] that accepts per-request [`crate::RequestOptions`].
    pub async fn list_with_options(
        &self,
        params: ListParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<WorkflowExperimentRunList, Error> {
        let path = "/v1/workflows/experiments/runs".to_string();
        let method = http::Method::GET;
        self.client
            .request_with_query_opts(method, &path, &params, options)
            .await
    }

    /// Returns an async [`futures_util::Stream`] that yields every `ExperimentRun`
    /// across all pages, advancing the `after` cursor under the hood.
    ///
    /// ```ignore
    /// use futures_util::TryStreamExt;
    /// let all: Vec<ExperimentRun> = self
    ///     .list_auto_paging(params)
    ///     .try_collect()
    ///     .await?;
    /// ```
    pub fn list_auto_paging(
        &self,
        params: ListParams,
    ) -> impl futures_util::Stream<Item = Result<ExperimentRun, Error>> + '_ {
        crate::pagination::auto_paginate_pages(move |after| {
            let mut params = params.clone();
            params.after = after;
            async move {
                let page = self.list(params).await?;
                Ok((page.data, page.list_metadata.after))
            }
        })
    }

    /// Create Experiment Run Flat
    ///
    /// Create an experiment run.
    ///
    /// The ``experiment_id`` and (optionally) ``workflow_id`` live in the
    /// body — flat-resource shape per meta-pattern-blueprint §1. When
    /// ``workflow_id`` is absent the experiment's stored workflow is used;
    /// when present it must match (the validation rejects mismatched pairs
    /// with 404, defending against confused-deputy callers).
    pub async fn create(&self, params: CreateParams) -> Result<ExperimentRun, Error> {
        self.create_with_options(params, None).await
    }

    /// Variant of [`Self::create`] that accepts per-request [`crate::RequestOptions`].
    pub async fn create_with_options(
        &self,
        params: CreateParams,
        options: Option<&crate::RequestOptions>,
    ) -> Result<ExperimentRun, Error> {
        let path = "/v1/workflows/experiments/runs".to_string();
        let method = http::Method::POST;
        self.client
            .request_with_body_opts(method, &path, &params, Some(&params.body), options)
            .await
    }

    /// Get Experiment Run
    pub async fn get(&self, run_id: &str) -> Result<ExperimentRun, Error> {
        self.get_with_options(run_id, None).await
    }

    /// Variant of [`Self::get`] that accepts per-request [`crate::RequestOptions`].
    pub async fn get_with_options(
        &self,
        run_id: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<ExperimentRun, Error> {
        let run_id = crate::client::path_segment(run_id);
        let path = format!("/v1/workflows/experiments/runs/{run_id}");
        let method = http::Method::GET;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }

    /// Cancel Experiment Run
    pub async fn cancel(&self, run_id: &str) -> Result<CancelWorkflowExperimentRunResponse, Error> {
        self.cancel_with_options(run_id, None).await
    }

    /// Variant of [`Self::cancel`] that accepts per-request [`crate::RequestOptions`].
    pub async fn cancel_with_options(
        &self,
        run_id: &str,
        options: Option<&crate::RequestOptions>,
    ) -> Result<CancelWorkflowExperimentRunResponse, Error> {
        let run_id = crate::client::path_segment(run_id);
        let path = format!("/v1/workflows/experiments/runs/{run_id}/cancel");
        let method = http::Method::POST;
        self.client
            .request_with_query_opts(method, &path, &(), options)
            .await
    }
}
