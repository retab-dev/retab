// This file is auto-generated by oagen. Do not edit.

package com.retab;

import static org.junit.jupiter.api.Assertions.assertInstanceOf;
import static org.junit.jupiter.api.Assertions.assertNotNull;

import com.fasterxml.jackson.databind.DeserializationFeature;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import com.retab.models.AnyItemMatchesCondition;
import com.retab.models.Condition;
import org.junit.jupiter.api.Test;

/**
 * Verifies a discriminated-union response deserializes into the correct concrete variant. {@code
 * any_item_matches} selects {@code AnyItemMatchesCondition}, which is NOT the first variant of the
 * union -- the case the previous emitter dropped.
 */
final class DiscriminatedUnionRoundTripTest {
  // Mirrors RetabClient.defaultObjectMapper() but tolerates absent required
  // creator fields so the test asserts polymorphic dispatch, not full payloads.
  private static final ObjectMapper MAPPER =
      new ObjectMapper()
          .registerModule(new JavaTimeModule())
          .configure(DeserializationFeature.FAIL_ON_NULL_CREATOR_PROPERTIES, false)
          .configure(DeserializationFeature.FAIL_ON_MISSING_CREATOR_PROPERTIES, false);

  @Test
  void nonFirstVariantDeserializesIntoCorrectConcreteType() throws Exception {
    String json = "{\"kind\":\"any_item_matches\",\"condition\":{\"kind\":\"exists\"}}";

    Condition value = MAPPER.readValue(json, Condition.class);

    // Jackson reconstructed the correct concrete variant from the discriminator.
    AnyItemMatchesCondition variant = assertInstanceOf(AnyItemMatchesCondition.class, value);
    assertNotNull(variant);
  }
}
