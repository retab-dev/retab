// This file is auto-generated by oagen. Do not edit.

package com.retab.workflowruns;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.JsonNode;
import com.retab.RetabClient;
import com.retab.models.CancelWorkflowResponse;
import com.retab.models.CreateWorkflowRunRequest;
import com.retab.models.MimeData;
import com.retab.models.WorkflowExportPayloadRequest;
import com.retab.models.WorkflowExportPayloadResponse;
import com.retab.models.WorkflowRun;
import com.retab.types.SortOrder;
import com.retab.types.WorkflowExportPayloadRequestExcludeStatus;
import com.retab.types.WorkflowExportPayloadRequestExportSource;
import com.retab.types.WorkflowExportPayloadRequestStatus;
import com.retab.types.WorkflowExportPayloadRequestTriggerType;
import com.retab.types.WorkflowRunsExcludeStatus;
import com.retab.types.WorkflowRunsStatus;
import com.retab.types.WorkflowRunsTriggerType;
import java.io.IOException;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

public final class WorkflowRunsApi {
  private final RetabClient client;

  public WorkflowRunsApi(RetabClient client) {
    this.client = client;
  }

  public RetabClient getClient() {
    return client;
  }

  public List<WorkflowRun> list() throws IOException, InterruptedException {
    return list(null, null, null, null, null, null, null, null, null, null, null, null, null, null);
  }

  public List<WorkflowRun> list(
      String workflowId,
      WorkflowRunsStatus status,
      WorkflowRunsExcludeStatus excludeStatus,
      WorkflowRunsTriggerType triggerType,
      String fromDate,
      String toDate,
      Long minDurationMs,
      Long maxDurationMs,
      String search,
      String before,
      String after,
      Long limit,
      SortOrder order,
      String sortBy)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/runs";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "status", status);
    appendQueryParam(query, "exclude_status", excludeStatus);
    appendQueryParam(query, "trigger_type", triggerType);
    appendQueryParam(query, "from_date", fromDate);
    appendQueryParam(query, "to_date", toDate);
    appendQueryParam(query, "min_duration_ms", minDurationMs);
    appendQueryParam(query, "max_duration_ms", maxDurationMs);
    appendQueryParam(query, "search", search);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "limit", limit);
    appendQueryParam(query, "order", order);
    appendQueryParam(query, "sort_by", sortBy);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(
            data.traverse(client.getObjectMapper()), new TypeReference<List<WorkflowRun>>() {});
  }

  public WorkflowRun create(CreateWorkflowRunRequest request)
      throws IOException, InterruptedException {
    return create(
        request == null ? null : request.getWorkflowId(),
        request == null ? null : request.getDocuments(),
        request == null ? null : request.getJsonInputs(),
        request == null ? null : request.getVersion());
  }

  public WorkflowRun create(
      String workflowId,
      Map<String, MimeData> documents,
      Map<String, Object> jsonInputs,
      String version)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/runs";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("workflow_id", workflowId);
    if (documents != null) {
      body.put("documents", documents);
    }
    if (jsonInputs != null) {
      body.put("json_inputs", jsonInputs);
    }
    if (version != null) {
      body.put("version", version);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowRun.class);
  }

  public WorkflowExportPayloadResponse export(WorkflowExportPayloadRequest request)
      throws IOException, InterruptedException {
    return export(
        request == null ? null : request.getWorkflowId(),
        request == null ? null : request.getBlockId(),
        request == null ? null : request.getExportSource(),
        request == null ? null : request.getSelectedRunIds(),
        request == null ? null : request.getSelectedDocTypes(),
        request == null ? null : request.getStatus(),
        request == null ? null : request.getExcludeStatus(),
        request == null ? null : request.getFromDate(),
        request == null ? null : request.getToDate(),
        request == null ? null : request.getTriggerType(),
        request == null ? null : request.getPreferredColumns(),
        request == null ? null : request.getDelimiter(),
        request == null ? null : request.getLineDelimiter(),
        request == null ? null : request.getQuote());
  }

  public WorkflowExportPayloadResponse export(
      String workflowId,
      String blockId,
      WorkflowExportPayloadRequestExportSource exportSource,
      List<String> selectedRunIds,
      List<String> selectedDocTypes,
      WorkflowExportPayloadRequestStatus status,
      WorkflowExportPayloadRequestExcludeStatus excludeStatus,
      String fromDate,
      String toDate,
      WorkflowExportPayloadRequestTriggerType triggerType,
      List<String> preferredColumns,
      String delimiter,
      String lineDelimiter,
      String quote)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/runs/export";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("workflow_id", workflowId);
    body.put("block_id", blockId);
    if (exportSource != null) {
      body.put("export_source", exportSource);
    }
    if (selectedRunIds != null) {
      body.put("selected_run_ids", selectedRunIds);
    }
    if (selectedDocTypes != null) {
      body.put("selected_doc_types", selectedDocTypes);
    }
    if (status != null) {
      body.put("status", status);
    }
    if (excludeStatus != null) {
      body.put("exclude_status", excludeStatus);
    }
    if (fromDate != null) {
      body.put("from_date", fromDate);
    }
    if (toDate != null) {
      body.put("to_date", toDate);
    }
    if (triggerType != null) {
      body.put("trigger_type", triggerType);
    }
    if (preferredColumns != null) {
      body.put("preferred_columns", preferredColumns);
    }
    if (delimiter != null) {
      body.put("delimiter", delimiter);
    }
    if (lineDelimiter != null) {
      body.put("line_delimiter", lineDelimiter);
    }
    if (quote != null) {
      body.put("quote", quote);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowExportPayloadResponse.class);
  }

  public WorkflowRun get(String runId) throws IOException, InterruptedException {
    String path = "/v1/workflows/runs/" + encodePathSegment(runId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowRun.class);
  }

  public Object delete(String runId) throws IOException, InterruptedException {
    String path = "/v1/workflows/runs/" + encodePathSegment(runId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("DELETE", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), Object.class);
  }

  public CancelWorkflowResponse cancel(String runId) throws IOException, InterruptedException {
    String path = "/v1/workflows/runs/" + encodePathSegment(runId) + "/cancel";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), CancelWorkflowResponse.class);
  }

  private static String encodePathSegment(Object value) {
    return URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20");
  }

  private static void appendQueryParam(StringBuilder query, String name, Object value) {
    if (value == null) {
      return;
    }
    if (query.length() > 0) {
      query.append("&");
    }
    query.append(URLEncoder.encode(name, StandardCharsets.UTF_8).replace("+", "%20"));
    query.append("=");
    query.append(
        URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20"));
  }

  private static String serializeParam(Object value) {
    if (value instanceof Enum<?>) {
      try {
        Object serialized = value.getClass().getMethod("getValue").invoke(value);
        if (serialized != null) {
          return String.valueOf(serialized);
        }
      } catch (ReflectiveOperationException ignored) {
        // Fall through to the enum name if the enum does not expose a wire value.
      }
    }
    return String.valueOf(value);
  }
}
