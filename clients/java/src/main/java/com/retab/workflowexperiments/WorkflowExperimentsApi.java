// This file is auto-generated by oagen. Do not edit.

package com.retab.workflowexperiments;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.JsonNode;
import com.retab.RetabClient;
import com.retab.experimentrunmetrics.ExperimentRunMetricsApi;
import com.retab.experimentrunresults.ExperimentRunResultsApi;
import com.retab.experimentruns.ExperimentRunsApi;
import com.retab.models.CreateExperimentRequest;
import com.retab.models.ExperimentDocumentCaptureRequest;
import com.retab.models.ExplicitExperimentDocumentRequest;
import com.retab.models.UpdateExperimentRequest;
import com.retab.models.WorkflowExperiment;
import com.retab.types.CreateExperimentRequestNConsensus;
import com.retab.types.SortOrder;
import com.retab.types.UpdateExperimentRequestNConsensus;
import java.io.IOException;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

public final class WorkflowExperimentsApi {
  private final RetabClient client;

  public WorkflowExperimentsApi(RetabClient client) {
    this.client = client;
  }

  public RetabClient getClient() {
    return client;
  }

  public ExperimentRunMetricsApi metrics() {
    return new ExperimentRunMetricsApi(client);
  }

  public ExperimentRunResultsApi results() {
    return new ExperimentRunResultsApi(client);
  }

  public ExperimentRunsApi runs() {
    return new ExperimentRunsApi(client);
  }

  public List<WorkflowExperiment> list(
      String workflowId, String before, String after, Long limit, SortOrder order)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/experiments";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "limit", limit);
    appendQueryParam(query, "order", order);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(
            data.traverse(client.getObjectMapper()),
            new TypeReference<List<WorkflowExperiment>>() {});
  }

  public WorkflowExperiment create(CreateExperimentRequest request)
      throws IOException, InterruptedException {
    return create(
        request == null ? null : request.getWorkflowId(),
        request == null ? null : request.getBlockId(),
        request == null ? null : request.getDocumentCaptures(),
        request == null ? null : request.getDocuments(),
        request == null ? null : request.getNConsensus(),
        request == null ? null : request.getName(),
        request == null ? null : request.getSourceExperimentId());
  }

  public WorkflowExperiment create(
      String workflowId,
      String blockId,
      List<ExperimentDocumentCaptureRequest> documentCaptures,
      List<ExplicitExperimentDocumentRequest> documents,
      CreateExperimentRequestNConsensus nConsensus,
      String name,
      String sourceExperimentId)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/experiments";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("workflow_id", workflowId);
    if (blockId != null) {
      body.put("block_id", blockId);
    }
    if (documentCaptures != null) {
      body.put("document_captures", documentCaptures);
    }
    if (documents != null) {
      body.put("documents", documents);
    }
    if (nConsensus != null) {
      body.put("n_consensus", nConsensus);
    }
    if (name != null) {
      body.put("name", name);
    }
    if (sourceExperimentId != null) {
      body.put("source_experiment_id", sourceExperimentId);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowExperiment.class);
  }

  public WorkflowExperiment get(String experimentId) throws IOException, InterruptedException {
    String path = "/v1/workflows/experiments/" + encodePathSegment(experimentId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowExperiment.class);
  }

  public WorkflowExperiment update(String experimentId, UpdateExperimentRequest request)
      throws IOException, InterruptedException {
    return update(
        experimentId,
        request == null ? null : request.getDocumentCaptures(),
        request == null ? null : request.getDocuments(),
        request == null ? null : request.getNConsensus(),
        request == null ? null : request.getName());
  }

  public WorkflowExperiment update(
      String experimentId,
      List<ExperimentDocumentCaptureRequest> documentCaptures,
      List<ExplicitExperimentDocumentRequest> documents,
      UpdateExperimentRequestNConsensus nConsensus,
      String name)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/experiments/" + encodePathSegment(experimentId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    if (documentCaptures != null) {
      body.put("document_captures", documentCaptures);
    }
    if (documents != null) {
      body.put("documents", documents);
    }
    if (nConsensus != null) {
      body.put("n_consensus", nConsensus);
    }
    if (name != null) {
      body.put("name", name);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("PATCH", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowExperiment.class);
  }

  public Object delete(String experimentId) throws IOException, InterruptedException {
    String path = "/v1/workflows/experiments/" + encodePathSegment(experimentId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("DELETE", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), Object.class);
  }

  private static String encodePathSegment(Object value) {
    return URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20");
  }

  private static void appendQueryParam(StringBuilder query, String name, Object value) {
    if (value == null) {
      return;
    }
    if (query.length() > 0) {
      query.append("&");
    }
    query.append(URLEncoder.encode(name, StandardCharsets.UTF_8).replace("+", "%20"));
    query.append("=");
    query.append(
        URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20"));
  }

  private static String serializeParam(Object value) {
    if (value instanceof Enum<?>) {
      try {
        Object serialized = value.getClass().getMethod("getValue").invoke(value);
        if (serialized != null) {
          return String.valueOf(serialized);
        }
      } catch (ReflectiveOperationException ignored) {
        // Fall through to the enum name if the enum does not expose a wire value.
      }
    }
    return String.valueOf(value);
  }
}
