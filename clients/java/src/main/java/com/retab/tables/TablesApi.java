// This file is auto-generated by oagen. Do not edit.

package com.retab.tables;

import com.retab.RetabClient;
import com.retab.models.BodyCreateTableV1TablesPost;
import com.retab.models.BodyReplaceTableV1TablesTableIdPut;
import com.retab.models.UpdateWorkflowTableRequest;
import com.retab.models.WorkflowTableListResponse;
import com.retab.models.WorkflowTableProfileResponse;
import com.retab.models.WorkflowTableResponse;
import com.retab.models.WorkflowTableRowsResponse;
import com.retab.models.WorkflowTableSchemaResponse;
import com.retab.models.WorkflowTableValidationColumnRule;
import com.retab.models.WorkflowTableValidationRequest;
import com.retab.models.WorkflowTableValidationResponse;
import java.io.IOException;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

public final class TablesApi {
  private final RetabClient client;

  public TablesApi(RetabClient client) {
    this.client = client;
  }

  public RetabClient getClient() {
    return client;
  }

  public WorkflowTableListResponse list() throws IOException, InterruptedException {
    String path = "/v1/tables";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableListResponse.class);
  }

  public WorkflowTableListResponse create(BodyCreateTableV1TablesPost request)
      throws IOException, InterruptedException {
    return create(
        request == null ? null : request.getName(),
        request == null ? null : request.getFile(),
        request == null ? null : request.getColumnSchemaOverrides());
  }

  public WorkflowTableListResponse create(String name, String file, String columnSchemaOverrides)
      throws IOException, InterruptedException {
    String path = "/v1/tables";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", name);
    body.put("file", file);
    if (columnSchemaOverrides != null) {
      body.put("column_schema_overrides", columnSchemaOverrides);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableListResponse.class);
  }

  public WorkflowTableResponse get(String tableId) throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableResponse.class);
  }

  public WorkflowTableListResponse replace(
      String tableId, BodyReplaceTableV1TablesTableIdPut request)
      throws IOException, InterruptedException {
    return replace(
        tableId,
        request == null ? null : request.getFile(),
        request == null ? null : request.getColumnSchemaOverrides());
  }

  public WorkflowTableListResponse replace(
      String tableId, String file, String columnSchemaOverrides)
      throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("file", file);
    if (columnSchemaOverrides != null) {
      body.put("column_schema_overrides", columnSchemaOverrides);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("PUT", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableListResponse.class);
  }

  public WorkflowTableListResponse update(String tableId, UpdateWorkflowTableRequest request)
      throws IOException, InterruptedException {
    return update(
        tableId,
        request == null ? null : request.getName(),
        request == null ? null : request.getMetadata());
  }

  public WorkflowTableListResponse update(String tableId, String name, Map<String, Object> metadata)
      throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    if (name != null) {
      body.put("name", name);
    }
    if (metadata != null) {
      body.put("metadata", metadata);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("PATCH", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableListResponse.class);
  }

  public Object delete(String tableId) throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("DELETE", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), Object.class);
  }

  public Object download(String tableId) throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId) + "/download";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), Object.class);
  }

  public WorkflowTableProfileResponse profile(String tableId, List<String> select)
      throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId) + "/profile";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "select", select);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableProfileResponse.class);
  }

  public WorkflowTableRowsResponse query(String tableId) throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId) + "/query";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableRowsResponse.class);
  }

  public WorkflowTableSchemaResponse schema(String tableId)
      throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId) + "/schema";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), WorkflowTableSchemaResponse.class);
  }

  public WorkflowTableValidationResponse validate(
      String tableId, WorkflowTableValidationRequest request)
      throws IOException, InterruptedException {
    return validate(
        tableId,
        request == null ? null : request.getRequiredColumns(),
        request == null ? null : request.getColumns(),
        request == null ? null : request.getUnique());
  }

  public WorkflowTableValidationResponse validate(
      String tableId,
      List<String> requiredColumns,
      Map<String, WorkflowTableValidationColumnRule> columns,
      List<List<String>> unique)
      throws IOException, InterruptedException {
    String path = "/v1/tables/" + encodePathSegment(tableId) + "/validate";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    if (requiredColumns != null) {
      body.put("required_columns", requiredColumns);
    }
    if (columns != null) {
      body.put("columns", columns);
    }
    if (unique != null) {
      body.put("unique", unique);
    }
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Api-Key", client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client
        .getObjectMapper()
        .readValue(response.body(), WorkflowTableValidationResponse.class);
  }

  private static String encodePathSegment(Object value) {
    return URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20");
  }

  private static void appendQueryParam(StringBuilder query, String name, Object value) {
    if (value == null) {
      return;
    }
    if (query.length() > 0) {
      query.append("&");
    }
    query.append(URLEncoder.encode(name, StandardCharsets.UTF_8).replace("+", "%20"));
    query.append("=");
    query.append(
        URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20"));
  }

  private static String serializeParam(Object value) {
    if (value instanceof Enum<?>) {
      try {
        Object serialized = value.getClass().getMethod("getValue").invoke(value);
        if (serialized != null) {
          return String.valueOf(serialized);
        }
      } catch (ReflectiveOperationException ignored) {
        // Fall through to the enum name if the enum does not expose a wire value.
      }
    }
    return String.valueOf(value);
  }
}
