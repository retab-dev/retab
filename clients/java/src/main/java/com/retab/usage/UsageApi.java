// This file is auto-generated by oagen. Do not edit.

package com.retab.usage;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.JsonNode;
import com.retab.RetabClient;
import com.retab.models.UsageBlockRecord;
import com.retab.models.UsagePrimitiveRecord;
import com.retab.models.UsageRunRecord;
import com.retab.types.SortOrder;
import java.io.IOException;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.List;

public final class UsageApi {
  private final RetabClient client;

  public UsageApi(RetabClient client) {
    this.client = client;
  }

  public RetabClient getClient() {
    return client;
  }

  public List<UsageBlockRecord> listBlocks() throws IOException, InterruptedException {
    return listBlocks(null, null, null, null, null, null, null, null);
  }

  public List<UsageBlockRecord> listBlocks(
      Long limit,
      SortOrder order,
      String before,
      String after,
      String workflowId,
      String blockType,
      String fromDate,
      String toDate)
      throws IOException, InterruptedException {
    String path = "/v1/usage/blocks";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "limit", limit);
    appendQueryParam(query, "order", order);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "block_type", blockType);
    appendQueryParam(query, "from_date", fromDate);
    appendQueryParam(query, "to_date", toDate);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(
            data.traverse(client.getObjectMapper()),
            new TypeReference<List<UsageBlockRecord>>() {});
  }

  public List<UsagePrimitiveRecord> listPrimitives() throws IOException, InterruptedException {
    return listPrimitives(
        null, null, null, null, null, null, null, null, null, null, null, null, null, null, null,
        null, null);
  }

  public List<UsagePrimitiveRecord> listPrimitives(
      Long limit,
      SortOrder order,
      String before,
      String after,
      String environmentId,
      String workflowId,
      String projectId,
      String apiKeyId,
      String accessTokenId,
      String userId,
      String runId,
      String blockId,
      String operation,
      String status,
      String metadata,
      String fromDate,
      String toDate)
      throws IOException, InterruptedException {
    String path = "/v1/usage/primitives";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "limit", limit);
    appendQueryParam(query, "order", order);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "environment_id", environmentId);
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "project_id", projectId);
    appendQueryParam(query, "api_key_id", apiKeyId);
    appendQueryParam(query, "access_token_id", accessTokenId);
    appendQueryParam(query, "user_id", userId);
    appendQueryParam(query, "run_id", runId);
    appendQueryParam(query, "block_id", blockId);
    appendQueryParam(query, "operation", operation);
    appendQueryParam(query, "status", status);
    appendQueryParam(query, "metadata", metadata);
    appendQueryParam(query, "from_date", fromDate);
    appendQueryParam(query, "to_date", toDate);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(
            data.traverse(client.getObjectMapper()),
            new TypeReference<List<UsagePrimitiveRecord>>() {});
  }

  public List<UsageRunRecord> listRuns() throws IOException, InterruptedException {
    return listRuns(null, null, null, null, null, null, null, null, null);
  }

  public List<UsageRunRecord> listRuns(
      Long limit,
      SortOrder order,
      String before,
      String after,
      String workflowId,
      String status,
      String triggerType,
      String fromDate,
      String toDate)
      throws IOException, InterruptedException {
    String path = "/v1/usage/runs";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "limit", limit);
    appendQueryParam(query, "order", order);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "status", status);
    appendQueryParam(query, "trigger_type", triggerType);
    appendQueryParam(query, "from_date", fromDate);
    appendQueryParam(query, "to_date", toDate);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(
            data.traverse(client.getObjectMapper()), new TypeReference<List<UsageRunRecord>>() {});
  }

  private static String encodePathSegment(Object value) {
    return URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20");
  }

  private static void appendQueryParam(StringBuilder query, String name, Object value) {
    if (value == null) {
      return;
    }
    if (query.length() > 0) {
      query.append("&");
    }
    query.append(URLEncoder.encode(name, StandardCharsets.UTF_8).replace("+", "%20"));
    query.append("=");
    query.append(
        URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20"));
  }

  private static String serializeParam(Object value) {
    if (value instanceof Enum<?>) {
      try {
        Object serialized = value.getClass().getMethod("getValue").invoke(value);
        if (serialized != null) {
          return String.valueOf(serialized);
        }
      } catch (ReflectiveOperationException ignored) {
        // Fall through to the enum name if the enum does not expose a wire value.
      }
    }
    return String.valueOf(value);
  }
}
