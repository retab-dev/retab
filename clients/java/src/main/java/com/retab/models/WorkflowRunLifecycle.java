// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over WorkflowRunLifecycle variants, selected by the {@code status}
 * discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * WorkflowRunLifecycle value = api.get(...);
 * if (value instanceof AwaitingReviewRun v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "status",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = AwaitingReviewRun.class, name = "awaiting_review"),
  @JsonSubTypes.Type(value = CancelledTerminal.class, name = "cancelled"),
  @JsonSubTypes.Type(value = CompletedTerminal.class, name = "completed"),
  @JsonSubTypes.Type(value = ErrorTerminal.class, name = "error"),
  @JsonSubTypes.Type(value = PendingRun.class, name = "pending"),
  @JsonSubTypes.Type(value = RunningRun.class, name = "running")
})
public sealed interface WorkflowRunLifecycle
    permits AwaitingReviewRun,
        CancelledTerminal,
        CompletedTerminal,
        ErrorTerminal,
        PendingRun,
        RunningRun {}
