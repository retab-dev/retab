// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over Experiment variants, selected by the {@code kind} discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * Experiment value = api.get(...);
 * if (value instanceof ExperimentSummaryMetricsResponse v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "kind",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = ExperimentSummaryMetricsResponse.class, name = "summary"),
  @JsonSubTypes.Type(value = ExperimentByDocumentMetricsResponse.class, name = "by_document"),
  @JsonSubTypes.Type(value = ExperimentByTargetMetricsResponse.class, name = "by_target"),
  @JsonSubTypes.Type(value = ExperimentVotesMetricsResponse.class, name = "votes"),
  @JsonSubTypes.Type(value = ExperimentMetricsStaleError.class, name = "stale_metrics"),
  @JsonSubTypes.Type(value = ExperimentMetricsMissingError.class, name = "no_metrics")
})
public sealed interface Experiment
    permits ExperimentSummaryMetricsResponse,
        ExperimentByDocumentMetricsResponse,
        ExperimentByTargetMetricsResponse,
        ExperimentVotesMetricsResponse,
        ExperimentMetricsStaleError,
        ExperimentMetricsMissingError {}
