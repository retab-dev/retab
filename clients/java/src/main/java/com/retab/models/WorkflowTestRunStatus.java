// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over WorkflowTestRunStatus variants, selected by the {@code status}
 * discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * WorkflowTestRunStatus value = api.get(...);
 * if (value instanceof CancelledWorkflowTestRun v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "status",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = CancelledWorkflowTestRun.class, name = "cancelled"),
  @JsonSubTypes.Type(value = CompletedWorkflowTestRun.class, name = "completed"),
  @JsonSubTypes.Type(value = ErrorWorkflowTestRun.class, name = "error"),
  @JsonSubTypes.Type(value = PendingWorkflowTestRun.class, name = "pending"),
  @JsonSubTypes.Type(value = QueuedWorkflowTestRun.class, name = "queued"),
  @JsonSubTypes.Type(value = RunningWorkflowTestRun.class, name = "running")
})
public sealed interface WorkflowTestRunStatus
    permits CancelledWorkflowTestRun,
        CompletedWorkflowTestRun,
        ErrorWorkflowTestRun,
        PendingWorkflowTestRun,
        QueuedWorkflowTestRun,
        RunningWorkflowTestRun {}
