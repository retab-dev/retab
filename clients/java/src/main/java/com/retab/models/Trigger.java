// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over Trigger variants, selected by the {@code type} discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * Trigger value = api.get(...);
 * if (value instanceof ApiTrigger v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "type",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = ApiTrigger.class, name = "api"),
  @JsonSubTypes.Type(value = EmailTrigger.class, name = "email"),
  @JsonSubTypes.Type(value = ManualTrigger.class, name = "manual"),
  @JsonSubTypes.Type(value = RestartTrigger.class, name = "restart"),
  @JsonSubTypes.Type(value = ScheduleTrigger.class, name = "schedule"),
  @JsonSubTypes.Type(value = WebhookTrigger.class, name = "webhook")
})
public sealed interface Trigger
    permits ApiTrigger,
        EmailTrigger,
        ManualTrigger,
        RestartTrigger,
        ScheduleTrigger,
        WebhookTrigger {}
