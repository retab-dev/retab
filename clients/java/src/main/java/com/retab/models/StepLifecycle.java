// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over StepLifecycle variants, selected by the {@code status} discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * StepLifecycle value = api.get(...);
 * if (value instanceof AwaitingReviewStepLifecycle v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "status",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = AwaitingReviewStepLifecycle.class, name = "awaiting_review"),
  @JsonSubTypes.Type(value = CancelledStepLifecycle.class, name = "cancelled"),
  @JsonSubTypes.Type(value = CompletedStepLifecycle.class, name = "completed"),
  @JsonSubTypes.Type(value = ErrorStepLifecycle.class, name = "error"),
  @JsonSubTypes.Type(value = PendingStepLifecycle.class, name = "pending"),
  @JsonSubTypes.Type(value = QueuedStepLifecycle.class, name = "queued"),
  @JsonSubTypes.Type(value = RunningStepLifecycle.class, name = "running"),
  @JsonSubTypes.Type(value = SkippedStepLifecycle.class, name = "skipped")
})
public sealed interface StepLifecycle
    permits AwaitingReviewStepLifecycle,
        CancelledStepLifecycle,
        CompletedStepLifecycle,
        ErrorStepLifecycle,
        PendingStepLifecycle,
        QueuedStepLifecycle,
        RunningStepLifecycle,
        SkippedStepLifecycle {}
