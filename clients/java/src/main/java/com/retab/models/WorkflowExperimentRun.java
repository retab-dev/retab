// This file is auto-generated by oagen. Do not edit.

package com.retab.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Discriminated union over WorkflowExperimentRun variants, selected by the {@code status}
 * discriminator.
 *
 * <p>Jackson deserializes the wire JSON into the correct concrete variant, so all variant-specific
 * fields round-trip losslessly. Branch on the concrete type to read variant data:
 *
 * <pre>{@code
 * WorkflowExperimentRun value = api.get(...);
 * if (value instanceof CancelledWorkflowExperimentRun v) {
 *     handle(v);
 * }
 * }</pre>
 */
@JsonTypeInfo(
    use = JsonTypeInfo.Id.NAME,
    include = JsonTypeInfo.As.EXISTING_PROPERTY,
    property = "status",
    visible = true)
@JsonSubTypes({
  @JsonSubTypes.Type(value = CancelledWorkflowExperimentRun.class, name = "cancelled"),
  @JsonSubTypes.Type(value = CompletedWorkflowExperimentRun.class, name = "completed"),
  @JsonSubTypes.Type(value = ErrorWorkflowExperimentRun.class, name = "error"),
  @JsonSubTypes.Type(value = PendingWorkflowExperimentRun.class, name = "pending"),
  @JsonSubTypes.Type(value = QueuedWorkflowExperimentRun.class, name = "queued"),
  @JsonSubTypes.Type(value = RunningWorkflowExperimentRun.class, name = "running")
})
public sealed interface WorkflowExperimentRun
    permits CancelledWorkflowExperimentRun,
        CompletedWorkflowExperimentRun,
        ErrorWorkflowExperimentRun,
        PendingWorkflowExperimentRun,
        QueuedWorkflowExperimentRun,
        RunningWorkflowExperimentRun {}
