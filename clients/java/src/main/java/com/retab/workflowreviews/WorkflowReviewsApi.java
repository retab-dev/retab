// This file is auto-generated by oagen. Do not edit.

package com.retab.workflowreviews;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.JsonNode;
import com.retab.RetabClient;
import com.retab.models.ApproveReviewRequest;
import com.retab.models.RejectReviewRequest;
import com.retab.models.Review;
import com.retab.models.SubmitDecisionResponse;
import com.retab.types.ReviewDecisionStatus;
import com.retab.workflowreviewversions.WorkflowReviewVersionsApi;
import java.io.IOException;
import java.net.URI;
import java.net.URLEncoder;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.nio.charset.StandardCharsets;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

public final class WorkflowReviewsApi {
  private final RetabClient client;

  public WorkflowReviewsApi(RetabClient client) {
    this.client = client;
  }

  public RetabClient getClient() {
    return client;
  }

  public WorkflowReviewVersionsApi versions() {
    return new WorkflowReviewVersionsApi(client);
  }

  public List<Review> list() throws IOException, InterruptedException {
    return list(null, null, null, null, null, null, null, null, null);
  }

  public List<Review> list(
      String workflowId,
      String runId,
      String blockId,
      String stepId,
      String iterationKey,
      ReviewDecisionStatus decisionStatus,
      String before,
      String after,
      Long limit)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/reviews";
    StringBuilder query = new StringBuilder();
    appendQueryParam(query, "workflow_id", workflowId);
    appendQueryParam(query, "run_id", runId);
    appendQueryParam(query, "block_id", blockId);
    appendQueryParam(query, "step_id", stepId);
    appendQueryParam(query, "iteration_key", iterationKey);
    appendQueryParam(query, "decision_status", decisionStatus);
    appendQueryParam(query, "before", before);
    appendQueryParam(query, "after", after);
    appendQueryParam(query, "limit", limit);
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    JsonNode root = client.getObjectMapper().readTree(response.body());
    JsonNode data = root.isArray() ? root : root.get("data");
    if (data == null || data.isNull()) {
      return List.of();
    }
    return client
        .getObjectMapper()
        .readValue(data.traverse(client.getObjectMapper()), new TypeReference<List<Review>>() {});
  }

  public Review get(String reviewId) throws IOException, InterruptedException {
    String path = "/v1/workflows/reviews/" + encodePathSegment(reviewId);
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.noBody();
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    HttpRequest httpRequest = requestBuilder.method("GET", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), Review.class);
  }

  public SubmitDecisionResponse approve(String reviewId, ApproveReviewRequest request)
      throws IOException, InterruptedException {
    return approve(reviewId, request == null ? null : request.getVersionId());
  }

  public SubmitDecisionResponse approve(String reviewId, String versionId)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/reviews/" + encodePathSegment(reviewId) + "/approve";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("version_id", versionId);
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), SubmitDecisionResponse.class);
  }

  public SubmitDecisionResponse reject(String reviewId, RejectReviewRequest request)
      throws IOException, InterruptedException {
    return reject(
        reviewId,
        request == null ? null : request.getVersionId(),
        request == null ? null : request.getReason());
  }

  public SubmitDecisionResponse reject(String reviewId, String versionId, String reason)
      throws IOException, InterruptedException {
    String path = "/v1/workflows/reviews/" + encodePathSegment(reviewId) + "/reject";
    StringBuilder query = new StringBuilder();
    URI uri = URI.create(client.getBaseUrl() + path + (query.length() == 0 ? "" : "?" + query));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("version_id", versionId);
    body.put("reason", reason);
    String requestBody = client.getObjectMapper().writeValueAsString(body);
    HttpRequest.BodyPublisher publisher = HttpRequest.BodyPublishers.ofString(requestBody);
    HttpRequest.Builder requestBuilder =
        HttpRequest.newBuilder(uri)
            .header("Accept", "application/json")
            .header("Authorization", "Bearer " + client.getApiKey());
    requestBuilder.header("Content-Type", "application/json");
    HttpRequest httpRequest = requestBuilder.method("POST", publisher).build();
    HttpResponse<String> response =
        client.getHttpClient().send(httpRequest, HttpResponse.BodyHandlers.ofString());
    if (response.statusCode() < 200 || response.statusCode() >= 300) {
      throw new IOException("Request failed (" + response.statusCode() + "): " + response.body());
    }
    if (response.body() == null || response.body().isBlank()) {
      return null;
    }
    return client.getObjectMapper().readValue(response.body(), SubmitDecisionResponse.class);
  }

  private static String encodePathSegment(Object value) {
    return URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20");
  }

  private static void appendQueryParam(StringBuilder query, String name, Object value) {
    if (value == null) {
      return;
    }
    if (query.length() > 0) {
      query.append("&");
    }
    query.append(URLEncoder.encode(name, StandardCharsets.UTF_8).replace("+", "%20"));
    query.append("=");
    query.append(
        URLEncoder.encode(serializeParam(value), StandardCharsets.UTF_8).replace("+", "%20"));
  }

  private static String serializeParam(Object value) {
    if (value instanceof Enum<?>) {
      try {
        Object serialized = value.getClass().getMethod("getValue").invoke(value);
        if (serialized != null) {
          return String.valueOf(serialized);
        }
      } catch (ReflectiveOperationException ignored) {
        // Fall through to the enum name if the enum does not expose a wire value.
      }
    }
    return String.valueOf(value);
  }
}
