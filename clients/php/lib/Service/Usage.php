<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace Retab\Service;

use Retab\Resource\UsageBlockRecord;
use Retab\Resource\UsagePrimitiveRecord;
use Retab\Resource\UsageRunRecord;

class Usage
{
    public function __construct(
        private readonly \Retab\HttpClient $client,
    ) {}

    /**
     * List Usage Blocks
     * @param int|null $limit Maximum number of block rows to return. Defaults to 20.
     * @param \Retab\Resource\EditsOrder $order Sort direction on block id. Defaults to "desc".
     * @param string|null $before Return blocks before this cursor (opaque token from a prior page's list_metadata).
     * @param string|null $after Return blocks after this cursor (opaque token from a prior page's list_metadata).
     * @param string|null $workflowId Filter to a single workflow id.
     * @param string|null $blockType Filter by block type (e.g. extract, classify, split, parse, edit, partition).
     * @param string|null $fromDate Inclusive activity lower bound (YYYY-MM-DD, UTC).
     * @param string|null $toDate Inclusive activity upper bound (YYYY-MM-DD, UTC).
     * @return \Retab\PaginatedResponse<\Retab\Resource\UsageBlockRecord>
     * @throws \Retab\Exception\RetabException
     */
    public function listBlocks(
        ?int $limit = null,
        \Retab\Resource\EditsOrder $order = \Retab\Resource\EditsOrder::Desc,
        ?string $before = null,
        ?string $after = null,
        ?string $workflowId = null,
        ?string $blockType = null,
        ?string $fromDate = null,
        ?string $toDate = null,
        ?\Retab\RequestOptions $options = null,
    ): \Retab\PaginatedResponse {
        $query = array_filter([
            'limit' => $limit,
            'order' => $order->value,
            'before' => $before,
            'after' => $after,
            'workflow_id' => $workflowId,
            'block_type' => $blockType,
            'from_date' => $fromDate,
            'to_date' => $toDate,
        ], fn($v) => $v !== null);
        return $this->client->requestPage(
            method: 'GET',
            path: 'v1/usage/blocks',
            query: $query,
            modelClass: UsageBlockRecord::class,
            options: $options,
        );
    }

    /**
     * List Usage Primitives
     * @param int|null $limit Maximum number of execution rows to return. Defaults to 20.
     * @param \Retab\Resource\EditsOrder $order Sort direction on execution created_at. Defaults to "desc".
     * @param string|null $before Return executions before this primitive-execution id (keyset cursor).
     * @param string|null $after Return executions after this primitive-execution id (keyset cursor).
     * @param string|null $environmentId Scope the export to this environment id within the caller's organization. Defaults to the authenticated identity's environment.
     * @param string|null $workflowId Filter to a single workflow id (origin workflow).
     * @param string|null $projectId Filter to executions owned by a single project id.
     * @param string|null $runId Filter to a single workflow run id (origin run).
     * @param string|null $blockId Filter to a single workflow block id (origin block).
     * @param string|null $operation Filter by operation (extraction, classify, split, parse, edit, schema_generation).
     * @param string|null $status Filter by execution lifecycle status.
     * @param string|null $metadata Filter by metadata equality: a JSON object of string key/value pairs (e.g. {"tenant":"acme"}). Pairs AND together.
     * @param string|null $fromDate Inclusive created_at lower bound (YYYY-MM-DD, UTC).
     * @param string|null $toDate Inclusive created_at upper bound (YYYY-MM-DD, UTC).
     * @return \Retab\PaginatedResponse<\Retab\Resource\UsagePrimitiveRecord>
     * @throws \Retab\Exception\RetabException
     */
    public function listPrimitives(
        ?int $limit = null,
        \Retab\Resource\EditsOrder $order = \Retab\Resource\EditsOrder::Desc,
        ?string $before = null,
        ?string $after = null,
        ?string $environmentId = null,
        ?string $workflowId = null,
        ?string $projectId = null,
        ?string $runId = null,
        ?string $blockId = null,
        ?string $operation = null,
        ?string $status = null,
        ?string $metadata = null,
        ?string $fromDate = null,
        ?string $toDate = null,
        ?\Retab\RequestOptions $options = null,
    ): \Retab\PaginatedResponse {
        $query = array_filter([
            'limit' => $limit,
            'order' => $order->value,
            'before' => $before,
            'after' => $after,
            'environment_id' => $environmentId,
            'workflow_id' => $workflowId,
            'project_id' => $projectId,
            'run_id' => $runId,
            'block_id' => $blockId,
            'operation' => $operation,
            'status' => $status,
            'metadata' => $metadata,
            'from_date' => $fromDate,
            'to_date' => $toDate,
        ], fn($v) => $v !== null);
        return $this->client->requestPage(
            method: 'GET',
            path: 'v1/usage/primitives',
            query: $query,
            modelClass: UsagePrimitiveRecord::class,
            options: $options,
        );
    }

    /**
     * List Usage Runs
     * @param int|null $limit Maximum number of run rows to return. Defaults to 20.
     * @param \Retab\Resource\EditsOrder $order Sort direction on run created_at. Defaults to "desc".
     * @param string|null $before Return runs before this run id (keyset cursor).
     * @param string|null $after Return runs after this run id (keyset cursor).
     * @param string|null $workflowId Filter to a single workflow id.
     * @param string|null $status Filter by lifecycle status: pending, queued, running, completed, error, failed, awaiting_review, or cancelled.
     * @param string|null $triggerType Filter by trigger type: manual, api, schedule, webhook, email, or restart.
     * @param string|null $fromDate Inclusive created_at lower bound (YYYY-MM-DD, UTC).
     * @param string|null $toDate Inclusive created_at upper bound (YYYY-MM-DD, UTC).
     * @return \Retab\PaginatedResponse<\Retab\Resource\UsageRunRecord>
     * @throws \Retab\Exception\RetabException
     */
    public function listRuns(
        ?int $limit = null,
        \Retab\Resource\EditsOrder $order = \Retab\Resource\EditsOrder::Desc,
        ?string $before = null,
        ?string $after = null,
        ?string $workflowId = null,
        ?string $status = null,
        ?string $triggerType = null,
        ?string $fromDate = null,
        ?string $toDate = null,
        ?\Retab\RequestOptions $options = null,
    ): \Retab\PaginatedResponse {
        $query = array_filter([
            'limit' => $limit,
            'order' => $order->value,
            'before' => $before,
            'after' => $after,
            'workflow_id' => $workflowId,
            'status' => $status,
            'trigger_type' => $triggerType,
            'from_date' => $fromDate,
            'to_date' => $toDate,
        ], fn($v) => $v !== null);
        return $this->client->requestPage(
            method: 'GET',
            path: 'v1/usage/runs',
            query: $query,
            modelClass: UsageRunRecord::class,
            options: $options,
        );
    }
}
