// Code generated by oagen. DO NOT EDIT.

package retab

import (
	"context"
	"fmt"
	"net/url"
)

// WorkflowTestRunResultService handles WorkflowTestRunResults operations.
type WorkflowTestRunResultService struct {
	client *Client
}

// WorkflowTestRunResultsListParams contains the parameters for List.
type WorkflowTestRunResultsListParams struct {
	PaginationParams
	RunID string `url:"run_id" json:"-"`
}

// List test Execution Results
// List workflow test results for a single run, page by page.
// Pagination strategy: the parent
// “workflow_test_runs.result_run_record_ids“ document already holds the
// ordered list of child record IDs. “workflow_block_test_runs“ rows do
// not carry a “run_id“ field (the relationship lives only on the
// parent), so a direct keyset query on the child collection is not
// possible without a schema change. We slice the parent's ordered list to
// resolve cursors and then “$in“-query the child collection for only
// the requested page — preserving the run-time ordering encoded in the
// parent doc and avoiding a fan-out collection scan.
func (s *WorkflowTestRunResultService) List(ctx context.Context, params *WorkflowTestRunResultsListParams, opts ...RequestOption) (*PaginatedList[WorkflowTestResult], error) {
	if params == nil {
		return nil, fmt.Errorf("retab: run_id is required")
	}
	if params.RunID == "" {
		return nil, fmt.Errorf("retab: run_id is required")
	}
	return doPaginated[WorkflowTestResult](ctx, s.client, "GET", "/v1/workflows/tests/results", params, nil, opts...)
}

// Get test Execution Result
func (s *WorkflowTestRunResultService) Get(ctx context.Context, resultID string, opts ...RequestOption) (*WorkflowTestResult, error) {
	if resultID == "" {
		return nil, fmt.Errorf("retab: result_id is required")
	}
	var result WorkflowTestResult
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/v1/workflows/tests/results/%s", url.PathEscape(resultID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
