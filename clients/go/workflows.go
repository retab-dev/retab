// Code generated by oagen. DO NOT EDIT.

package retab

import (
	"context"
	"fmt"
	"net/url"
)

// WorkflowService handles Workflows operations.
type WorkflowService struct {
	client      *Client
	Artifacts   *WorkflowArtifactService
	Blocks      *WorkflowBlockService
	Edges       *WorkflowEdgeService
	Experiments *WorkflowExperimentService
	Reviews     *WorkflowReviewService
	Runs        *WorkflowRunService
	Spec        *WorkflowSpecService
	Steps       *WorkflowStepService
	Tests       *WorkflowTestService
}

// WorkflowsListParams contains the parameters for List.
type WorkflowsListParams struct {
	PaginationParams
	// Defaults to "updated_at".
	SortBy *string `url:"sort_by,omitempty" json:"-"`
}

// List workflows
// List workflows with pagination and optional filtering.
func (s *WorkflowService) List(ctx context.Context, params *WorkflowsListParams, opts ...RequestOption) (*PaginatedList[Workflow], error) {
	return doPaginated[Workflow](ctx, s.client, "GET", "/v1/workflows", params, nil, opts...)
}

// WorkflowsCreateParams contains the parameters for Create.
type WorkflowsCreateParams struct {
	// Name is the name of the workflow
	Name *string `json:"name,omitempty" url:"-"`
	// Description is description of the workflow
	Description *string `json:"description,omitempty" url:"-"`
}

// Create workflow
// Create a new workflow.
// The workflow starts unpublished and is scaffolded with a default
// "Document" input block in the live block collection.
func (s *WorkflowService) Create(ctx context.Context, params *WorkflowsCreateParams, opts ...RequestOption) (*Workflow, error) {
	var result Workflow
	_, err := s.client.request(ctx, "POST", "/v1/workflows", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// Get workflow
// Get a single workflow by ID.
// Returns workflow metadata only.
func (s *WorkflowService) Get(ctx context.Context, workflowID string, opts ...RequestOption) (*Workflow, error) {
	if workflowID == "" {
		return nil, fmt.Errorf("retab: workflow_id is required")
	}
	var result Workflow
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/v1/workflows/%s", url.PathEscape(workflowID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// WorkflowsUpdateParams contains the parameters for Update.
type WorkflowsUpdateParams struct {
	// Name is the name of the workflow
	Name *string `json:"name,omitempty" url:"-"`
	// Description is description of the workflow
	Description *string `json:"description,omitempty" url:"-"`
}

// Update workflow
// Update an existing workflow.
func (s *WorkflowService) Update(ctx context.Context, workflowID string, params *WorkflowsUpdateParams, opts ...RequestOption) (*Workflow, error) {
	if workflowID == "" {
		return nil, fmt.Errorf("retab: workflow_id is required")
	}
	var result Workflow
	_, err := s.client.request(ctx, "PATCH", fmt.Sprintf("/v1/workflows/%s", url.PathEscape(workflowID)), nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// Delete workflow
// Delete a workflow and all its associated entities.
// This deletes:
// - The workflow document
// - All blocks and edges (live collections)
// - All block and edge snapshots
// - All workflow snapshots
func (s *WorkflowService) Delete(ctx context.Context, workflowID string, opts ...RequestOption) error {
	if workflowID == "" {
		return fmt.Errorf("retab: workflow_id is required")
	}
	_, err := s.client.request(ctx, "DELETE", fmt.Sprintf("/v1/workflows/%s", url.PathEscape(workflowID)), nil, nil, nil, opts)
	return err
}

// DiscardDraft workflow
// Discard all draft changes and restore the workflow to its published state.
// This operation:
// 1. Recreates blocks and edges from the published version
// 2. Updates the workflow's updated_at timestamp and current draft graph
// Requires the workflow to be published (have a published_version_id).
func (s *WorkflowService) DiscardDraft(ctx context.Context, workflowID string, opts ...RequestOption) (*Workflow, error) {
	if workflowID == "" {
		return nil, fmt.Errorf("retab: workflow_id is required")
	}
	var result Workflow
	_, err := s.client.request(ctx, "POST", fmt.Sprintf("/v1/workflows/%s/discard-draft", url.PathEscape(workflowID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// WorkflowsPublishParams contains the parameters for Publish.
type WorkflowsPublishParams struct {
	Body interface{} `json:"-"`
}

// Publish workflow
// Publish a workflow.
// This creates an immutable snapshot of the workflow configuration, making it available for workflow runs.
// The live entities remain unchanged so users can continue editing.
func (s *WorkflowService) Publish(ctx context.Context, workflowID string, params *WorkflowsPublishParams, opts ...RequestOption) (*Workflow, error) {
	if workflowID == "" {
		return nil, fmt.Errorf("retab: workflow_id is required")
	}
	var result Workflow
	_, err := s.client.request(ctx, "POST", fmt.Sprintf("/v1/workflows/%s/publish", url.PathEscape(workflowID)), nil, params.Body, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
