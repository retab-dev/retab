// Code generated by oagen. DO NOT EDIT.

package retab

import (
	"context"
	"fmt"
	"net/url"
)

// WorkflowSpecService handles WorkflowSpecs operations.
type WorkflowSpecService struct {
	client *Client
}

// WorkflowSpecsValidateParams contains the parameters for Validate.
type WorkflowSpecsValidateParams struct {
	// YamlDefinition is workflow YAML definition
	YamlDefinition string `json:"yaml_definition" url:"-"`
}

// Validate workflow Spec
// Validate declarative YAML without mutating workflow state.
// Contract:
// - validate, plan, and apply agree on whether a spec is acceptable: any
// severity=error diagnostic — whether emitted at parse time as a
// DeclarativeWorkflowError or bumped during compile/diagnose — raises
// HTTP 400 with the structured error issues
// - warnings do not make the document invalid; warning-only specs return
// HTTP 200 with `is_valid=True` and the warning issues in `diagnostics`
// - counts reflect the canonical compiled graph, not raw authored block count
func (s *WorkflowSpecService) Validate(ctx context.Context, params *WorkflowSpecsValidateParams, opts ...RequestOption) (*DeclarativeValidationResponse, error) {
	var result DeclarativeValidationResponse
	_, err := s.client.request(ctx, "POST", "/v1/workflows/spec/validate", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// WorkflowSpecsPlanParams contains the parameters for Plan.
type WorkflowSpecsPlanParams struct {
	// YamlDefinition is workflow YAML definition
	YamlDefinition string `json:"yaml_definition" url:"-"`
}

// Plan workflow Spec
// Compute the declarative reconcile plan against the current draft workflow.
// Contract:
// - plan compares authored YAML against current draft state
// - canonical exported YAML should plan as `noop` against the same draft
func (s *WorkflowSpecService) Plan(ctx context.Context, params *WorkflowSpecsPlanParams, opts ...RequestOption) (*DeclarativePlanResponse, error) {
	var result DeclarativePlanResponse
	_, err := s.client.request(ctx, "POST", "/v1/workflows/spec/plan", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// WorkflowSpecsApplyParams contains the parameters for Apply.
type WorkflowSpecsApplyParams struct {
	// YamlDefinition is workflow YAML definition
	YamlDefinition string `json:"yaml_definition" url:"-"`
}

// Apply workflow Spec
// Apply declarative YAML to draft workflow state.
// Contract:
// - apply writes canonical draft state, not authored formatting
// - re-applying canonical exported YAML against unchanged draft state should
// return an empty resource_changes list
func (s *WorkflowSpecService) Apply(ctx context.Context, params *WorkflowSpecsApplyParams, opts ...RequestOption) (*DeclarativeApplyResponse, error) {
	var result DeclarativeApplyResponse
	_, err := s.client.request(ctx, "POST", "/v1/workflows/spec/apply", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// Get export Workflow Spec
// Export draft workflow state as canonical declarative YAML.
func (s *WorkflowSpecService) Get(ctx context.Context, workflowID string, opts ...RequestOption) (*DeclarativeExportResponse, error) {
	if workflowID == "" {
		return nil, fmt.Errorf("retab: workflow_id is required")
	}
	var result DeclarativeExportResponse
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/v1/workflows/spec/%s", url.PathEscape(workflowID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
