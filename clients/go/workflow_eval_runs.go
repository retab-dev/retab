// Code generated by oagen. DO NOT EDIT.

package retab

import (
	"context"
	"fmt"
	"net/url"
	"time"
)

// WorkflowEvalRunService handles WorkflowEvalRuns operations.
type WorkflowEvalRunService struct {
	client *Client
}

// WorkflowEvalRunsListParams contains the parameters for List.
type WorkflowEvalRunsListParams struct {
	PaginationParams
	WorkflowID    *string    `url:"workflow_id,omitempty" json:"-"`
	EvalID        *string    `url:"eval_id,omitempty" json:"-"`
	TargetBlockID *string    `url:"target_block_id,omitempty" json:"-"`
	Status        *string    `url:"status,omitempty" json:"-"`
	ExcludeStatus *string    `url:"exclude_status,omitempty" json:"-"`
	TriggerType   *string    `url:"trigger_type,omitempty" json:"-"`
	FromDate      *time.Time `url:"from_date,omitempty" json:"-"`
	ToDate        *time.Time `url:"to_date,omitempty" json:"-"`
	// Defaults to "created_at".
	SortBy *string `url:"sort_by,omitempty" json:"-"`
}

// List workflow Eval Runs
// List workflow eval runs.
// Optionally filter by `workflow_id`, `eval_id`, `target_block_id`,
// `status`/`exclude_status`, `trigger_type`, and a `from_date`/`to_date`
// window. Returns a cursor-paginated list ordered by `sort_by` (default
// newest first).
func (s *WorkflowEvalRunService) List(ctx context.Context, params *WorkflowEvalRunsListParams, opts ...RequestOption) (*PaginatedList[WorkflowEvalRun], error) {
	return doPaginated[WorkflowEvalRun](ctx, s.client, "GET", "/v1/workflows/evals/runs", params, nil, opts...)
}

// WorkflowEvalRunsCreateParams contains the parameters for Create.
type WorkflowEvalRunsCreateParams struct {
	WorkflowID string `json:"workflow_id" url:"-"`
	// Scope is optional execution scope. Omit to run every saved eval in the workflow.
	Scope *WorkflowEvalRunScope `json:"scope,omitempty" url:"-"`
}

// Create workflow Eval Run
// Create a workflow-scoped eval run.
// `workflow_id` is the execution context. Optional `scope` narrows the
// run to one saved eval or one block; omitted scope runs all workflow evals.
func (s *WorkflowEvalRunService) Create(ctx context.Context, params *WorkflowEvalRunsCreateParams, opts ...RequestOption) (*WorkflowEvalRun, error) {
	var result WorkflowEvalRun
	_, err := s.client.request(ctx, "POST", "/v1/workflows/evals/runs", nil, params, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// Get workflow Eval Run
// Retrieve a single workflow eval run.
// Identified by `run_id`. Returns the run with its lifecycle status, timing,
// and pass/fail counts. Returns 404 if no run with that ID exists.
func (s *WorkflowEvalRunService) Get(ctx context.Context, runID string, opts ...RequestOption) (*WorkflowEvalRun, error) {
	if runID == "" {
		return nil, fmt.Errorf("retab: run_id is required")
	}
	var result WorkflowEvalRun
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/v1/workflows/evals/runs/%s", url.PathEscape(runID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}

// Cancel workflow Eval Run
// Cancel a workflow eval run.
// Identified by `run_id`. Stops the run and returns it with its updated
// cancelled lifecycle. Returns 404 if the run does not exist or is not in a
// cancellable state.
func (s *WorkflowEvalRunService) Cancel(ctx context.Context, runID string, opts ...RequestOption) (*WorkflowEvalRun, error) {
	if runID == "" {
		return nil, fmt.Errorf("retab: run_id is required")
	}
	var result WorkflowEvalRun
	_, err := s.client.request(ctx, "POST", fmt.Sprintf("/v1/workflows/evals/runs/%s/cancel", url.PathEscape(runID)), nil, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
