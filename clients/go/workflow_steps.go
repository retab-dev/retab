// Code generated by oagen. DO NOT EDIT.

package retab

import (
	"context"
	"fmt"
	"net/url"
)

// WorkflowStepService handles WorkflowSteps operations.
type WorkflowStepService struct {
	client *Client
}

// WorkflowStepsListParams contains the parameters for List.
type WorkflowStepsListParams struct {
	PaginationParams
	// RunID is optional workflow run ID filter.
	RunID *string `url:"run_id,omitempty" json:"-"`
	// BlockID is optional logical block ID filter.
	BlockID *string `url:"block_id,omitempty" json:"-"`
	// StepID is optional step ID filter.
	StepID *string `url:"step_id,omitempty" json:"-"`
	// BlockType is optional block type filter. Repeat the query parameter for multiple values.
	BlockType []string `url:"block_type,omitempty" json:"-"`
	// Status is optional step lifecycle status filter. Repeat the query parameter for multiple values.
	Status []string `url:"status,omitempty" json:"-"`
}

// List workflow Run Steps
// List steps with status and artifact summaries.
// Sorted by “started_at“ ascending with “step_id“ as the tiebreaker
// (the same compound key the underlying index uses). Pass “after“ for
// the next page, “before“ for the previous page — mutually exclusive.
// “run_id“ is optional; when omitted the list is scoped to the caller's
// organization.
func (s *WorkflowStepService) List(ctx context.Context, params *WorkflowStepsListParams, opts ...RequestOption) (*PaginatedList[WorkflowRunStep], error) {
	return doPaginated[WorkflowRunStep](ctx, s.client, "GET", "/v1/workflows/steps", params, nil, opts...)
}

// WorkflowStepsGetParams contains the parameters for Get.
type WorkflowStepsGetParams struct {
	// RunID is optional workflow run ID disambiguator.
	RunID *string `url:"run_id,omitempty" json:"-"`
}

// Get workflow Step
// Get one persisted step document by step id.
// This is the canonical step object shape used by “GET /workflows/steps“.
// It intentionally does not join fingerprint rows or return experiment query
// projections.
func (s *WorkflowStepService) Get(ctx context.Context, stepID string, params *WorkflowStepsGetParams, opts ...RequestOption) (*WorkflowRunStep, error) {
	if stepID == "" {
		return nil, fmt.Errorf("retab: step_id is required")
	}
	var result WorkflowRunStep
	_, err := s.client.request(ctx, "GET", fmt.Sprintf("/v1/workflows/steps/%s", url.PathEscape(stepID)), params, nil, &result, opts)
	if err != nil {
		return nil, err
	}
	return &result, nil
}
