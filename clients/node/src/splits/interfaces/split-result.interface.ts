// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type { SheetRegion, SheetRegionResponse } from './sheet-region.interface.js';
import {
  ZSheetRegion,
  deserializeSheetRegion,
  serializeSheetRegion,
} from './sheet-region.interface.js';

export interface SplitResult {
  /** The name of the subdocument */
  name: string;
  /** The pages of the subdocument (1-indexed) */
  pages: number[];
  /** @default [] */
  regions?: SheetRegion[];
}

export interface SplitResultResponse {
  name: string;
  pages: number[];
  regions?: SheetRegionResponse[];
}

export const ZSplitResult = z.object({
  name: z.string(),
  pages: z.number().int().array(),
  regions: ZSheetRegion.array().optional(),
}) as z.ZodType<SplitResult>;

export function deserializeSplitResult(wire: SplitResultResponse): SplitResult {
  return {
    name: wire['name'],
    pages: wire['pages'],
    regions:
      wire['regions'] == null
        ? (wire['regions'] as undefined)
        : wire['regions'].map((__i) => deserializeSheetRegion(__i)),
  };
}

export function serializeSplitResult(domain: SplitResult): SplitResultResponse {
  return {
    name: domain['name'],
    pages: domain['pages'],
    regions:
      domain['regions'] == null
        ? (domain['regions'] as undefined)
        : domain['regions'].map((__i) => serializeSheetRegion(__i)),
  };
}
