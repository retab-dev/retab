// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type {
  ReconstructEnrichedRow,
  ReconstructEnrichedRowResponse,
} from './reconstruct-enriched-row.interface.js';
import {
  ZReconstructEnrichedRow,
  deserializeReconstructEnrichedRow,
  serializeReconstructEnrichedRow,
} from './reconstruct-enriched-row.interface.js';

export interface ReconstructEnrichedTable {
  csv: string;
  header: string[];
  label: string;
  rows: ReconstructEnrichedRow[];
}

export interface ReconstructEnrichedTableResponse {
  csv: string;
  header: string[];
  label: string;
  rows: ReconstructEnrichedRowResponse[];
}

export const ZReconstructEnrichedTable = z.object({
  csv: z.string(),
  header: z.string().array(),
  label: z.string(),
  rows: ZReconstructEnrichedRow.array(),
}) as z.ZodType<ReconstructEnrichedTable>;

export function deserializeReconstructEnrichedTable(
  wire: ReconstructEnrichedTableResponse
): ReconstructEnrichedTable {
  return {
    csv: wire['csv'],
    header: wire['header'],
    label: wire['label'],
    rows: wire['rows'].map((__i) => deserializeReconstructEnrichedRow(__i)),
  };
}

export function serializeReconstructEnrichedTable(
  domain: ReconstructEnrichedTable
): ReconstructEnrichedTableResponse {
  return {
    csv: domain['csv'],
    header: domain['header'],
    label: domain['label'],
    rows: domain['rows'].map((__i) => serializeReconstructEnrichedRow(__i)),
  };
}
