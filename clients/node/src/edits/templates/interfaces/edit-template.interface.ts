// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type { FileRef, FileRefResponse } from '../../../extractions/interfaces/file-ref.interface.js';
import { ZFileRef, deserializeFileRef } from '../../../extractions/interfaces/file-ref.interface.js';
import type { FormField, FormFieldResponse } from '../../../workflows/artifacts/interfaces/form-field.interface.js';
import { ZFormField, deserializeFormField } from '../../../workflows/artifacts/interfaces/form-field.interface.js';

export interface EditTemplate {
  /** Unique identifier of the template. */
  id: string;
  /** Name of the template. */
  name: string;
  /** File information for the empty PDF template. */
  file: FileRef;
  /** Form fields attached to the template. */
  formFields?: FormField[];
  /**
   * Number of form fields in the template.
   * @default 0
   */
  fieldCount?: number;
  /** Timestamp of creation. */
  createdAt: Date;
  /** Timestamp of last update. */
  updatedAt: Date;
}

export interface EditTemplateResponse {
  id: string;
  name: string;
  file: FileRefResponse;
  form_fields?: FormFieldResponse[];
  field_count?: number;
  created_at: string;
  updated_at: string;
}

export const ZEditTemplate = z.object({
  "id": z.string(),
  "name": z.string(),
  "file": ZFileRef,
  "formFields": ZFormField.array().optional(),
  "fieldCount": z.number().int().optional(),
  "createdAt": z.coerce.date(),
  "updatedAt": z.coerce.date(),
}) as z.ZodType<EditTemplate>;

export function deserializeEditTemplate(wire: EditTemplateResponse): EditTemplate {
  return {
    id: wire["id"],
    name: wire["name"],
    file: deserializeFileRef(wire["file"]),
    formFields: wire["form_fields"] == null ? (wire["form_fields"] as undefined) : wire["form_fields"].map((__i) => deserializeFormField(__i)),
    fieldCount: wire["field_count"],
    createdAt: new Date(wire["created_at"]),
    updatedAt: new Date(wire["updated_at"]),
  };
}
