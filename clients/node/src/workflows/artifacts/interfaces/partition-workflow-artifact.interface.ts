// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type { FileRef, FileRefResponse } from '../../../extractions/interfaces/file-ref.interface.js';
import { ZFileRef, deserializeFileRef } from '../../../extractions/interfaces/file-ref.interface.js';
import type { PartitionChunk, PartitionChunkResponse } from '../../../partitions/interfaces/partition-chunk.interface.js';
import { ZPartitionChunk, deserializePartitionChunk } from '../../../partitions/interfaces/partition-chunk.interface.js';
import type { PartitionConsensus, PartitionConsensusResponse } from '../../../partitions/interfaces/partition-consensus.interface.js';
import { ZPartitionConsensus, deserializePartitionConsensus } from '../../../partitions/interfaces/partition-consensus.interface.js';
import type { RetabUsage, RetabUsageResponse } from '../../../extractions/interfaces/retab-usage.interface.js';
import { ZRetabUsage, deserializeRetabUsage } from '../../../extractions/interfaces/retab-usage.interface.js';

export interface PartitionWorkflowArtifact {
  /** Unique identifier of the partition */
  id: string;
  /** Information about the partitioned file */
  file: FileRef;
  /** Model used for the partition operation */
  model: string;
  /** Partition key used for the run */
  key: string;
  /** Free-form instructions supplied with the partition request */
  instructions?: string | null;
  /**
   * Number of consensus votes used
   * @default 1
   */
  nConsensus?: number;
  /**
   * Whether pages were allowed to appear in more than one partition chunk
   * @default true
   */
  allowOverlap?: boolean;
  /** The list of partition chunks with their assigned pages */
  output?: PartitionChunk[];
  /** Consensus metadata for multi-vote partition runs */
  consensus?: PartitionConsensus;
  /** Usage information for the partition operation */
  usage?: RetabUsage | null;
  /** When this artifact was written by the orchestrator. */
  createdAt: Date;
  /**
   * Artifact operation that determines the backing record type
   * @default "partition"
   */
  operation: 'partition';
}

export interface PartitionWorkflowArtifactResponse {
  id: string;
  file: FileRefResponse;
  model: string;
  key: string;
  instructions?: string | null;
  n_consensus?: number;
  allow_overlap?: boolean;
  output?: PartitionChunkResponse[];
  consensus?: PartitionConsensusResponse;
  usage?: RetabUsageResponse | null;
  created_at: string;
  operation: 'partition';
}

export const ZPartitionWorkflowArtifact = z.object({
  "id": z.string(),
  "file": ZFileRef,
  "model": z.string(),
  "key": z.string(),
  "instructions": z.string().nullable().optional(),
  "nConsensus": z.number().int().optional(),
  "allowOverlap": z.boolean().optional(),
  "output": ZPartitionChunk.array().optional(),
  "consensus": ZPartitionConsensus.optional(),
  "usage": ZRetabUsage.nullable().optional(),
  "createdAt": z.coerce.date(),
  "operation": z.literal('partition'),
}) as z.ZodType<PartitionWorkflowArtifact>;

export function deserializePartitionWorkflowArtifact(wire: PartitionWorkflowArtifactResponse): PartitionWorkflowArtifact {
  return {
    id: wire["id"],
    file: deserializeFileRef(wire["file"]),
    model: wire["model"],
    key: wire["key"],
    instructions: wire["instructions"],
    nConsensus: wire["n_consensus"],
    allowOverlap: wire["allow_overlap"],
    output: wire["output"] == null ? (wire["output"] as undefined) : wire["output"].map((__i) => deserializePartitionChunk(__i)),
    consensus: wire["consensus"] == null ? (wire["consensus"] as undefined) : deserializePartitionConsensus(wire["consensus"]),
    usage: wire["usage"] == null ? (wire["usage"] as undefined) : (wire["usage"] == null ? wire["usage"] : deserializeRetabUsage(wire["usage"])),
    createdAt: new Date(wire["created_at"]),
    operation: wire["operation"],
  };
}
