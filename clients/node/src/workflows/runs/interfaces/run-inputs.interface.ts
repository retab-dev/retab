// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type {
  FileRef,
  FileRefResponse,
} from '../../../extractions/interfaces/file-ref.interface.js';
import {
  ZFileRef,
  deserializeFileRef,
  serializeFileRef,
} from '../../../extractions/interfaces/file-ref.interface.js';

/** Input payloads supplied at run creation time. */
export interface RunInputs {
  /** start_document block ID -> input document reference */
  documents?: Record<string, FileRef>;
  /** start-json block ID -> input JSON data */
  jsonData?: Record<string, unknown>;
}

export interface RunInputsResponse {
  documents?: Record<string, FileRefResponse>;
  json_data?: Record<string, unknown>;
}

export const ZRunInputs = z.object({
  documents: z.record(z.string(), ZFileRef).optional(),
  jsonData: z.record(z.string(), z.unknown()).optional(),
}) as z.ZodType<RunInputs>;

export function deserializeRunInputs(wire: RunInputsResponse): RunInputs {
  return {
    documents:
      wire['documents'] == null
        ? (wire['documents'] as undefined)
        : Object.fromEntries(
            Object.entries(wire['documents']).map(([__k, __v]) => [__k, deserializeFileRef(__v)])
          ),
    jsonData: wire['json_data'],
  };
}

export function serializeRunInputs(domain: RunInputs): RunInputsResponse {
  return {
    documents:
      domain['documents'] == null
        ? (domain['documents'] as undefined)
        : Object.fromEntries(
            Object.entries(domain['documents']).map(([__k, __v]) => [__k, serializeFileRef(__v)])
          ),
    json_data: domain['jsonData'],
  };
}
