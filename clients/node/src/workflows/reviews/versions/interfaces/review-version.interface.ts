// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type { Actor, ActorResponse } from '../../../../workflows/reviews/interfaces/actor.interface.js';
import { ZActor, deserializeActor } from '../../../../workflows/reviews/interfaces/actor.interface.js';

/** Public API shape for one immutable review version. */
export interface ReviewVersion {
  id: string;
  reviewId: string;
  parentId?: string | null;
  /** Actor that created the version. */
  author: Actor;
  snapshot: Record<string, unknown>;
  note?: string | null;
  /** When the review version was created. */
  createdAt: Date;
}

export interface ReviewVersionResponse {
  id: string;
  review_id: string;
  parent_id?: string | null;
  author: ActorResponse;
  snapshot: Record<string, unknown>;
  note?: string | null;
  created_at: string;
}

export const ZReviewVersion = z.object({
  "id": z.string(),
  "reviewId": z.string(),
  "parentId": z.string().nullable().optional(),
  "author": ZActor,
  "snapshot": z.record(z.string(), z.unknown()),
  "note": z.string().nullable().optional(),
  "createdAt": z.coerce.date(),
}) as z.ZodType<ReviewVersion>;

export function deserializeReviewVersion(wire: ReviewVersionResponse): ReviewVersion {
  return {
    id: wire["id"],
    reviewId: wire["review_id"],
    parentId: wire["parent_id"],
    author: deserializeActor(wire["author"]),
    snapshot: wire["snapshot"],
    note: wire["note"],
    createdAt: new Date(wire["created_at"]),
  };
}
