// This file is auto-generated by the Retab oagen emitter. Do not edit by hand.

import { z } from 'zod';
import type {
  FileRef,
  FileRefResponse,
} from '../../../extractions/interfaces/file-ref.interface.js';
import {
  ZFileRef,
  deserializeFileRef,
} from '../../../extractions/interfaces/file-ref.interface.js';
import type { HandlePayloadType } from './handle-payload-type.interface.js';
import { ZHandlePayloadType } from './handle-payload-type.interface.js';

/** Payload for a single block output handle. */
export interface HandlePayload {
  /** Type of payload */
  type: HandlePayloadType;
  /** For file handles: document reference */
  document?: FileRef | null;
  /** For JSON handles: structured data */
  data?: unknown;
  /** For json_ref handles: pointer to artifact-storage JSON body */
  artifactRef?: Record<string, unknown> | null;
  /** For json_ref handles: lightweight preview of the body */
  preview?: Record<string, unknown> | null;
}

export interface HandlePayloadResponse {
  type: HandlePayloadType;
  document?: FileRefResponse | null;
  data?: unknown;
  artifact_ref?: Record<string, unknown> | null;
  preview?: Record<string, unknown> | null;
}

export const ZHandlePayload = z.object({
  type: ZHandlePayloadType,
  document: ZFileRef.nullable().optional(),
  data: z.unknown().optional(),
  artifactRef: z.record(z.string(), z.unknown()).nullable().optional(),
  preview: z.record(z.string(), z.unknown()).nullable().optional(),
}) as z.ZodType<HandlePayload>;

export function deserializeHandlePayload(wire: HandlePayloadResponse): HandlePayload {
  return {
    type: wire['type'],
    document:
      wire['document'] == null
        ? (wire['document'] as undefined)
        : wire['document'] == null
          ? wire['document']
          : deserializeFileRef(wire['document']),
    data: wire['data'],
    artifactRef: wire['artifact_ref'],
    preview: wire['preview'],
  };
}
