interface:
  display_name: "Retab"
  short_description: "Build with Retab document APIs"
  default_prompt: "Use $retab to add Retab parse, extract, split, edit, classify, or workflow run calls to this codebase."
